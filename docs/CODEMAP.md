# CODEMAP — Bản đồ tổng thể Marketing OS (web + Telegram)

> Tự sinh từ knowledge graph. Commit `14cca9cc8e` · phân tích 2026-06-18T06:47:23Z.
> Nền tảng chiến lược marketing dùng AI cho founder & chủ doanh nghiệp Việt Nam, kết hợp bot Telegram với các pipeline multi-agent (market research, phân tích đối thủ, customer insight, tâm lý giá, social listening, chiến lược) và một web dashboard.

> **Mục đích:** tra nhanh "mục X nằm ở đâu" để sửa đúng chỗ (surgical), không phải đọc lại cả 2 hệ.
> Quy mô: 9 layer · 114 file · 582 function · 43 class · 33 bảng dữ liệu.

## Mục lục layer

- **Telegram Bot Interface** (`layer:bot-telegram`) — Cửa ngõ python-telegram-bot của sản phẩm: handler message/callback, inline keyboard, renderer phản hồi, đọc báo cáo Excel, và sinh báo cáo HTML phục vụ trải nghiệm hội thoại cho founder Việt N…
- **Multi-Agent Strategy Pipeline** (`layer:agents-strategy`) — Bộ não marketing-intelligence cốt lõi: orchestrator và các agent theo từng giai đoạn (discovery, market/competitor research, customer insight, tâm lý giá, soc…
- **Web Dashboard & Chat API** (`layer:webapp-dashboard`) — Backend FastAPI/Starlette vận hành web dashboard và trợ lý chat Max: REST endpoint, event streaming, store dựa trên Supabase/SQLite, business/a…
- **Static Web Frontend** (`layer:web-frontend`) — Client dashboard HTML/CSS/JS tĩnh (bản standalone và bản hosted) hiển thị dữ liệu campaign, content, ads được webapp API phục vụ.
- **Storage & Data Model** (`layer:storage-data`) — Tầng lưu trữ bao gồm các domain model (Session, BusinessProfile, BrandVoice), các repository v2 dựa trên Supabase (brief, campaign, engagement, post, pr…
- **Integrations, Background Jobs & Tool Belt** (`layer:services-tools`) — Các năng lực cross-cutting dùng bởi agent và bot: service Facebook OAuth/Ads cùng worker nền notifier/scheduler của nó, theo dõi đối thủ và …
- **Test Suite** (`layer:tests`) — Bộ test end-to-end kiểm chứng pipeline orchestrator, pipeline sinh content, web API, và hành vi menu persona.
- **Configuration, Deployment & Product Docs** (`layer:config-deploy-docs`) — Các entry point runtime ở root và file env/config (config.py, run_local.py, run_web.py, .env.example, railway.json, Procfile), bản deploy GitHub Pages…
- **AI Dev-Assistant Tooling** (`layer:project-tooling`) — Tài sản plugin Claude Code đóng gói trong repo (.claude/) — agent tùy biến, slash command, và bộ skill hỗ trợ phát triển project này, không thuộc phần …


---

## 🎯 BẢN ĐỒ OUTPUT (web ⇄ tele) — sửa đúng chỗ

> Mục output chung dùng chung skill prompt. **Riêng SYNTHESIS tách 2 prompt** —
> đây là chỗ dễ sửa nhầm nhất.

### Intake / Discovery (thu hồ sơ)
| Mặt | Sinh ở | Prompt | Render |
|-----|--------|--------|--------|
| **Web — AI-adaptive** | `agents/discovery.py:run_discovery_turn` | `agents/discovery_prompts.py` `MCKINSEY_INTERVIEW_SYSTEM` (L14) | `web/app.js` `aiIntakeView/aiIntakeSend` |
| **Web — wizard tĩnh** (fallback) | `web/app.js` `INTAKE_STEPS` (L204) + `handleIntake` (L425) | — (câu hỏi cứng trong JS) | `web/app.js` `staticWizard` (L261) |
| **Telegram** | `agents/discovery.py:run_discovery_turn` (dùng chung) | `MCKINSEY_INTERVIEW_SYSTEM` (+ legacy `agents/prompts.py`) | `agents/discovery.py:render_brief_card` |
| Lưu | `storage/v2/profiles.py` (`upsert_profile`), `webapp/business.py:save_profile/intake_turn` | | |

### 6 phân tích (DÙNG CHUNG cả 2 mặt — sửa 1 nơi)
| Output | Skill class (`agents/skills.py`) | System prompt |
|--------|----------------------------------|---------------|
| 🌐 Market research | `MarketResearchSkill` (L~108) | `MARKET_RESEARCH_SYSTEM` (`agents/prompts.py`) |
| 🥊 Competitor | `CompetitorSkill` (L~131) | `COMPETITOR_SYSTEM` |
| 👤 Customer insight | `CustomerInsightSkill` (L~162) | `CUSTOMER_INSIGHT_SYSTEM` |
| 💲 Pricing & tâm lý | `PsychologyPricingSkill` (L~178) | `PSYCHOLOGY_PRICING_SYSTEM` |
| ⚖️ SWOT | `SwotSkill` (L~349) | `SWOT_SYSTEM` |
| (phụ) USP / Social listening / Tactical | `UspDefinition/SocialListening/TacticalPlaybook` | trong `prompts.py` |

Render web: `web/app.js` `agentSection()` (L160) nhúng doc reader + trang `P.market/P.competitor/...` (L520+). Render tele: `bot/renderers.py`, `agents/output_formats.py`.

### 🔴 SYNTHESIS — chiến lược tổng hợp (TÁCH 2 PROMPT — QUAN TRỌNG)
| Mặt | Hàm chạy | **Prompt** | Output dạng |
|-----|----------|-----------|-------------|
| **WEB** (non-interactive, D-015) | `agents/pipeline.py:run_targeted_pipeline` → `agents/skills.py:StrategySynthesisSkill` (L304) | **`agents/prompts.py:STRATEGY_SYNTHESIZER_SYSTEM` (L618)** | **Markdown/HTML 10 phần** (Exec Summary, USP, SAVE, SMART, Roadmap, Channel/Budget, KPI, Retention, Quick Wins, Risks) |
| **TELE** (interactive + 8 câu chốt hướng) | `agents/strategy.py` (L61,99) | **`agents/strategy_prompts.py:CMO_STRATEGY_SYSTEM` (L9)** | **JSON có cấu trúc** (positioning, wedge, roadmap_90d, budget_allocation, content_pillars, kpi_dashboard, kill_criteria) |

→ **Muốn đổi chiến lược trên WEB:** sửa `STRATEGY_SYNTHESIZER_SYSTEM` (prompts.py:618) + `StrategySynthesisSkill` (skills.py:304). **ĐỪNG** đụng `CMO_STRATEGY_SYSTEM`.
→ **Muốn đổi chiến lược JSON / cầu nối campaign (tele):** sửa `CMO_STRATEGY_SYSTEM` (strategy_prompts.py) + `agents/strategy.py`. **ĐỪNG** đụng `STRATEGY_SYNTHESIZER_SYSTEM`.

### Cầu nối Campaign (chỉ TELE hiện có — web M1 sẽ tái dùng)
- `agents/campaign_intake.py:build_campaign_draft_from_strategy` — kéo `roadmap_90d`+budget từ JSON synthesis (CMO) → pre-fill draft. **Đây là chỗ SMART chốt số (D-029).**
- `agents/campaign_ideation.py` — sinh câu hỏi dịp/ý tưởng campaign.

### Điểm KHÁC giữa 2 mặt
- **Tele có, web chưa:** 8 câu chốt hướng giữa research→synthesis; campaign bridge (intake/ideation); brand voice intake; synthesis dạng JSON.
- **Web có, tele không:** doc reader/editor (sửa tay + nhờ Max chỉnh từng đoạn, version) `web/app.js` `P.doc`/`loadDoc`/`renderDoc` (L445+) + `webapp/business.py` `patch_skill_run/save_skill_edit/list_skill_versions`.

### Điều phối chạy agent (web)
`webapp/business.py:run_agent` → `_execute` → `run_targeted_pipeline`. Job in-memory đẩy SSE qua `webapp/events.py:hub`. API: `webapp/api.py:biz_agent_run` (`POST /api/biz/agent`).

---

## Telegram Bot Interface
_Cửa ngõ python-telegram-bot của sản phẩm: handler message/callback, inline keyboard, renderer phản hồi, đọc báo cáo Excel, và sinh báo cáo HTML vận hành trải nghiệm hội thoại cho founder Việt N…_

### `bot/handlers.py`
Module dispatch command và message/callback chính của bot Telegram — implement toàn bộ slash command, xử lý hội thoại văn bản tự do, xử lý upload ảnh/tài liệu, và luồng UI nhiều bước lớn …
  - 🔧 `send_long_message` L224-245 — Chia và gửi các tin nhắn Telegram vượt quá giới hạn ký tự của platform thành nhiều phần.
  - 🔧 `cmd_start` L251-326 — Xử lý lệnh /start, chào người dùng và khởi tạo hoặc tiếp tục session của họ.
  - 🔧 `cmd_settings` L330-356 — Xử lý lệnh /settings, hiển thị và cho phép thay đổi tùy chọn người dùng.
  - 🔧 `cmd_reset` L360-385 — Xử lý lệnh /reset, xóa trạng thái session hiện tại của người dùng.
  - 🔧 `cmd_dbg_funnel` L394-414 — Lệnh debug dump trạng thái funnel map hiện tại để chẩn đoán.
  - 🔧 `cmd_video_script_gen` L418-427 — Xử lý lệnh /video_script, khởi chạy task sinh video script.
  - 🔧 `cmd_content_generator` L431-440 — Xử lý lệnh /content, khởi chạy workflow content generator.
  - 🔧 `handle_message` L446-906 — Bộ dispatch tin nhắn văn bản tự do trung tâm, định tuyến văn bản người dùng gửi tới luồng phù hợp…
  - 🔧 `handle_photo` L910-960 — Xử lý ảnh người dùng tải lên, định tuyến vào luồng sửa ảnh hoặc phân tích đang hoạt động.
  - 🔧 `handle_document` L969-1041 — Xử lý tài liệu/file người dùng tải lên (ví dụ template Excel) và định tuyến vào …
  - 🔧 `handle_callback` L1278-1307 — Điểm vào cho các callback query của inline keyboard, ủy quyền cho callback nội bộ lớn …
  - 🔧 `cmd_admin_addquota` L8794-8830 — Lệnh admin tăng quota sử dụng của một người dùng.
  - 🔧 `cmd_admin_setquota` L8834-8868 — Lệnh admin đặt quota sử dụng của một người dùng về một giá trị cụ thể.
  - 🔧 `cmd_admin_resetusage` L8872-8901 — Lệnh admin reset các bộ đếm usage đã ghi nhận.
  - 🔧 `cmd_admin_userinfo` L8905-8948 — Lệnh admin hiển thị thông tin tài khoản/usage chi tiết của một người dùng cho trước.
  - 🔧 `cmd_history` L8955-9003 — Xử lý lệnh /history, hiển thị các output đã sinh trước đó của người dùng.
  - 🔧 `cmd_post` L9237-9267 — Xử lý lệnh /post, khởi động luồng viết bài đăng độc lập.
  - 🔧 `cmd_connect_ads` L9694-9753 — Xử lý lệnh /connect_ads, bắt đầu luồng OAuth để liên kết tài khoản ads Facebook/Meta.
  - 🔧 `cmd_switch_account` L9756-9793 — Xử lý lệnh /switch_account, cho phép người dùng chuyển đổi giữa các tài khoản ads đã liên kết.
  - 🔧 `cmd_disconnect_ads` L9796-9811 — Xử lý lệnh /disconnect_ads, gỡ liên kết tài khoản ads.
  - 🔧 `cmd_ads_settings` L9814-9862 — Xử lý lệnh /ads_settings, hiển thị và chỉnh sửa cấu hình tài khoản ads.
  - 🔧 `cmd_ads_analytics` L9894-9896 — Xử lý lệnh /ads_analytics, khởi chạy task báo cáo ads analytics.
  - 🔧 `cmd_ads_optimizer` L9899-9901 — Xử lý lệnh /ads_optimizer, khởi chạy task tối ưu chi tiêu ads.

### `bot/keyboards.py`
Xây dựng layout inline keyboard Telegram (menu hành động, control hoàn tất giai đoạn, nút post-action) dùng xuyên suốt UI hội thoại của bot.
  - 🔧 `post_action_keyboard` L408-420 — Xây dựng inline keyboard các nút hành động hiển thị kèm một bài đăng đã sinh (ví dụ adapt…

### `bot/renderers.py`
Chuyển đổi output LLM dạng chiến lược/cấu trúc và vận hành thành các định dạng hiển thị cho người dùng — thẻ tin nhắn Telegram, file báo cáo Markdown, và template Excel nhiều sheet kèm parse bảng và…
  - 🔧 `parse_operational_deliverable` L71-96 — Parse văn bản output thô từ LLM của một skill vận hành thành đối tượng deliverable có cấu trúc.
  - 🔧 `parse_operational_analysis` L99-118 — Parse một output LLM dạng phân tích vận hành thành các phần có cấu trúc để hiển thị.
  - 🔧 `format_telegram_card` L136-182 — Định dạng một deliverable đã parse thành thẻ tin nhắn Telegram gọn gàng gồm headline, summary,…
  - 🔧 `render_markdown_file` L189-227 — Render một file báo cáo Markdown đầy đủ từ deliverable đã parse để tải xuống/xuất file.
  - 🔧 `_safe_sheet_name` L263-277 — Chuẩn hóa một chuỗi thành tên worksheet Excel hợp lệ, xử lý giới hạn độ dài và ký tự cấm…
  - 🔧 `_pivot_keyvalue_tables` L298-329 — Biến đổi các bảng Markdown dạng key-value thành dạng bảng pivot phù hợp để render Excel.
  - 🔧 `_split_table_by_week` L332-360 — Tách một bảng content-calendar tổng hợp thành các bảng riêng theo từng tuần cho Excel nhiều sheet.
  - 🔧 `_parse_post_batch_narrative` L367-478 — Parse một batch các bài đăng được sinh dạng tường thuật từ văn bản LLM thô thành các đối tượng có cấu trúc riêng…
  - 🔧 `render_template_excel` L486-618 — Render một deliverable có cấu trúc thành định dạng template Excel định trước kèm sheet được style…
  - 🔧 `render_excel_file` L621-868 — Hàm xuất Excel cấp cao nhất, chọn chiến lược render phù hợp và tạo ra file.
  - 🔧 `_haiku_rebuild_table` L871-928 — Dùng một lệnh gọi LLM nhanh (Haiku) để sửa các bảng Markdown lỗi không parse được theo chuẩn nghiêm ngặt.
  - 🔧 `_extract_markdown_tables` L931-987 — Trích xuất toàn bộ các block bảng Markdown từ một phản hồi văn bản thô để parse/render ở bước sau…

### `bot/main.py`
Entry point chế độ webhook cho bot Telegram; xây dựng Application của python-telegram-bot, đăng ký toàn bộ handler command/message/callback, gắn route web dashboard và OAuth callback lên một sin…
  - 🔧 `_build_app` L64-105 — Khởi tạo Application của python-telegram-bot và đăng ký toàn bộ handler command, message, và ca…
  - 🔧 `telegram_webhook` L113-118 — Route handler của Starlette nhận các request POST webhook Telegram đến và chuyển tiếp…
  - 🔧 `oauth_fb_callback` L121-124 — Route handler cho redirect OAuth Facebook, ủy quyền cho handle_callback để hoàn tất luồng.
  - 🔧 `dashboard` L132-134 — Route handler redirect request tới static web dashboard.
  - 🔧 `lifespan` L145-174 — Context manager lifespan của ứng dụng Starlette, khởi tạo DB pool, web store, và …
  - 🔧 `main` L191-198 — Kiểm tra cấu hình môi trường cần thiết và khởi động server uvicorn chạy con…

### `bot/web_control.py`
Handler lệnh Telegram cho phép người dùng xem và điều khiển trạng thái web dashboard (status, campaign, mục theo dõi, optimization, alert) trực tiếp từ chat.
  - 🔧 `cmd_web_help` L19-29 — Trả lời bằng thông báo trợ giúp liệt kê các lệnh /web_* điều khiển dashboard hiện có.
  - 🔧 `cmd_web_status` L32-43 — Báo cáo trạng thái dashboard hiện tại (mục theo dõi, job, optimization, alert, backend …
  - 🔧 `cmd_web_campaign` L46-53 — Thêm một campaign mới vào store dashboard từ tham số lệnh Telegram.
  - 🔧 `cmd_web_track` L56-63 — Thêm một mục theo dõi mới vào store dashboard từ tham số lệnh Telegram.
  - 🔧 `cmd_web_optimize` L66-74 — Liệt kê các gợi ý optimization đang chờ và xóa cái đầu tiên khi được yêu cầu qua Teleg…
  - 🔧 `cmd_web_alerts` L77-83 — Liệt kê các alert đang hoạt động từ trạng thái dashboard để phản hồi lệnh Telegram.
  - 🔧 `register` L86-93 — Đăng ký toàn bộ handler lệnh /web_* định nghĩa trong module này vào python-telegram-bot…

### `bot/__init__.py`
Initializer package rỗng đánh dấu thư mục bot/ là một Python package.

### `bot/excel_reader.py`
Đọc file .xlsx được tải lên và chuyển nội dung worksheet thành bảng markdown, kèm phát hiện skill dựa trên tên file để định tuyến spreadsheet tải lên tới đúng workflow task.
  - 🔧 `detect_skill_from_filename` L27-48 — Suy ra task/skill mà một file tải lên thuộc về bằng cách khớp phần tên file với…
  - 🔧 `_cell_to_str` L51-57 — Chuyển một giá trị cell Excel thành chuỗi sạch, chuẩn hóa số nguyên và loại bỏ …
  - 🔧 `_sheet_to_markdown` L60-88 — Chuyển một worksheet openpyxl thành chuỗi bảng markdown, xây dựng header và dòng phân tách…
  - 🔧 `read_xlsx_to_markdown` L91-115 — Đọc một luồng byte .xlsx trong bộ nhớ và chuyển mọi worksheet thành một bảng markdown tổng hợp…

### `bot/html_report.py`
Sinh báo cáo HTML đầy đủ về chiến lược marketing và ads-dashboard gửi tới người dùng, bao gồm chuyển đổi markdown sang HTML, layout nhiều giai đoạn dạng tab, biểu đồ thanh/bảng, và một arc do LLM sinh…
  - 🔧 `_ensure_blank_line_before_tables` L494-509 — Chèn một dòng trống trước các block bảng markdown để renderer markdown parse đúng…
  - 🔧 `_md_to_html` L512-529 — Chuyển một block văn bản markdown sang HTML, áp dụng các fix khoảng cách bảng và xử lý hậu kỳ…
  - 🔧 `parse_agent_output` L532-609 — Parse văn bản output thô của agent LLM thành các trường có cấu trúc (insight, detail, summary, v.v.)…
  - 🔧 `render_stage_html` L612-657 — Render markup HTML cho một tab giai đoạn chiến lược, gồm insight, detail, và…
  - 🔧 `_generate_tab_css` L660-671 — Sinh các rule CSS cần thiết để ẩn/hiện N panel tab trong báo cáo nhiều giai đoạn dạng tab.
  - 🔧 `render_bar_chart` L674-693 — Render một biểu đồ thanh ngang HTML từ danh sách các mục metric có nhãn, scale độ rộng thanh…
  - 🔧 `render_ads_table` L696-712 — Render một bảng HTML các metric ads từ định nghĩa cột và dữ liệu dòng.
  - 🔧 `render_insight_grid` L715-720 — Render một lưới HTML hai cột đối chiếu các thẻ insight 'good' và 'warning' cho một báo cáo ads…
  - 🔧 `build_ads_dashboard_report` L723-772 — Lắp ghép báo cáo HTML ads-dashboard hoàn chỉnh bằng cách kết hợp biểu đồ thanh, bảng metric,…
  - 🔧 `build_single_skill_report` L775-858 — Xây dựng báo cáo HTML độc lập cho output của một marketing skill, render từng p…
  - 🔧 `_section_mentions_archetype` L897-904 — Kiểm tra xem văn bản của một giai đoạn đã parse có nhắc tới từ khóa brand archetype đã biết nào không.
  - 🔧 `_resolve_or_none` L952-961 — Thử resolve một brand archetype từ ngành và văn bản tín hiệu, trả về None nếu không resolve được…
  - 🔧 `_extract_context_snippets` L964-992 — Lấy các đoạn context ngắn từ các giai đoạn chiến lược đã parse để đưa vào LLM banne…
  - 🔧 `_template_banner_data` L995-1005 — Xây dựng dữ liệu copy banner dự phòng (không qua LLM) từ một template tiếng Việt tĩnh đã khai…
  - 🔧 `_generate_banner_copy_via_llm` L1008-1095 — Gọi LLM router để sinh copy banner chẩn đoán/tác động đã địa phương hóa cho archetype…
  - 🔧 `_compose_banner_html` L1098-1127 — Soạn markup HTML cuối cùng cho banner archetype từ copy banner đã sinh hoặc từ template…
  - 🔧 `generate_archetype_banner_html` L1130-1152 — Entry point bất đồng bộ resolve archetype, sinh copy banner qua LLM (rơi về dự phòng…
  - 🔧 `render_archetype_banner_sync` L1155-1165 — Bản dự phòng đồng bộ render banner archetype chỉ dùng dữ liệu template tĩnh.
  - 🔧 `build_report` L1168-1241 — Entry point cấp cao nhất lắp ghép báo cáo HTML chiến lược marketing nhiều giai đoạn hoàn chỉnh…


---

## Multi-Agent Strategy Pipeline
_Bộ não marketing-intelligence cốt lõi: orchestrator và các agent theo từng giai đoạn (discovery, market/competitor research, customer insight, tâm lý giá, social listening, campaign ideation/intake/execution, funnel ma…_

### `agents/max_conductor.py`
Định tuyến tin nhắn tự do của người dùng thành intent + tham số bằng một lệnh gọi LLM Anthropic, dùng để dispatch các yêu cầu follow-up trong bot Telegram (ví dụ 'đổi channel sang Instagram').
  - 🔧 `_get_client` L25-33 — Khởi tạo lười và cache một instance AsyncAnthropic client ở cấp module.
  - 🔧 `route_intent` L59-113 — Gửi tin nhắn văn bản tự do của người dùng và context session tới Claude, parse phản hồi JSON…

### `agents/post_actions.py`
Parse các content calendar đã sinh thành các bài đăng có cấu trúc và cung cấp các hành động dựa trên LLM (sửa, chuyển đổi sang channel khác, sinh variant) cùng định dạng preview cho Telegram.
  - 🔧 `_get_client` L26-30 — Khởi tạo lười và cache một instance AsyncAnthropic client ở cấp module.
  - 🔧 `parse_calendar_to_posts` L35-88 — Tách văn bản calendar thô thành các block bài đăng riêng và gắn nhãn channel phát hiện được cho mỗi block,…
  - 🔧 `_detect_channel` L91-101 — Phát hiện theo kinh nghiệm (heuristic) channel marketing (ví dụ Facebook, Instagram) được nhắc tới trong một bl…
  - 🔧 `_detect_week_day` L104-121 — Trích xuất số tuần và nhãn ngày trong tuần từ một block văn bản calendar bằng regex và từ khóa…
  - 🔧 `_detect_pillar` L124-134 — Phân loại một block content vào một pillar marketing (educate/trust/engage/convert) dựa trên…
  - 🔧 `edit_post` L145-161 — Gọi LLM để sửa nội dung một bài đăng hiện có theo hướng dẫn văn bản tự do.
  - 🔧 `adapt_post` L179-199 — Gọi LLM để viết lại nội dung một bài đăng cho một channel mục tiêu khác, dùng session…
  - 🔧 `gen_variant` L213-229 — Sinh một variant thay thế cho nội dung một bài đăng qua LLM, dùng business của session…
  - 🔧 `format_post_preview` L234-250 — Định dạng một dict bài đăng thành chuỗi preview dễ đọc (kèm metadata) để hiển thị trong T…

### `agents/tone_calibration.py`
Triển khai luồng hiệu chỉnh tone brand-voice: parse một bài đăng mẫu từ calendar, sinh các bài đăng mẫu, trích xuất tín hiệu tone từ feedback người dùng, và sinh lại content/calendar để khớp th…
  - 🔧 `_get_client` L26-30 — Khởi tạo lười và cache một instance AsyncAnthropic client ở cấp module.
  - 🔧 `parse_first_post` L35-151 — Parse dòng bài đăng đầu tiên dạng bảng markdown trong một content calendar đã sinh bằng…
  - 🔧 `generate_sample_post` L172-235 — Sinh một bài đăng mẫu qua LLM cho một dòng calendar cho trước, dùng profile brand voice…
  - 🔧 `extract_tone_signals` L256-299 — Dùng LLM để trích xuất tín hiệu tone có cấu trúc (ví dụ style ưa thích, từ cấm) từ…
  - 🔧 `regen_post_with_signals` L310-349 — Sinh lại nội dung một bài đăng qua LLM, áp dụng các tín hiệu tone đã trích xuất như hướng dẫn bổ sung…
  - 🔧 `apply_tone_to_calendar` L372-413 — Áp dụng các tín hiệu tone đã hiệu chỉnh lên toàn bộ content calendar đã sinh qua LLM…
  - 🔧 `format_signals_card` L418-429 — Định dạng các tín hiệu tone đã trích xuất thành thẻ tóm tắt dễ đọc để hiển thị cho người dùng trong Te…

### `agents/__init__.py`
Marker package cho module agents, re-export orchestrator pipeline làm điểm vào công khai.

### `agents/agent_wrappers.py`
Các hàm wrapper bất đồng bộ mỏng bao quanh các skill AI chiến lược (market research, phân tích đối thủ, pricing, SWOT, retention/winback, synthesis), chuyển đổi lệnh gọi provider sang interface agent của pipeline…

### `agents/content_suite_prompts.py`
Các hằng số template prompt tĩnh cho bộ content generation (bài đăng, caption, video script) được các skill vận hành sử dụng.

### `agents/critic.py`
Chạy một lượt critic chất lượng dựa trên LLM trên output của agent, phát hiện các artifact meta-review và chèn hyperlink tới các nguồn đã biết trước khi trả về kết quả đã review.

### `agents/operational_prompts.py`
Thư viện lớn các hằng số template prompt tĩnh cho các skill marketing vận hành (ads, sales script, email/Zalo sequence, UGC brief).

### `agents/operational_skill.py`
Định nghĩa các class cơ sở OperationalSkillConfig và OperationalSkill, thiết lập hợp đồng (xây dựng context, xây dựng user message, theo dõi trường intake) cho mọi skill marketing vận hành.
  - 🧱 `OperationalSkillConfig` L22-39 — Đối tượng dữ liệu cấu hình mô tả metadata (tên, trường intake, định dạng output) cho một…
  - 🧱 `OperationalSkill` L42-263 — Class cơ sở cho các skill marketing vận hành, cung cấp xây dựng context/user-message và…

### `agents/operational_skills_config.py`
Module registry và factory trung tâm định nghĩa toàn bộ các class skill vận hành cụ thể (campaign brief, content calendar, ads copy, video script, so sánh đối thủ, brand positioning) và một bảng tra…
  - 🧱 `ContentGeneratorPipeline` L221-291 — Skill dạng pipeline pre-fill dữ liệu intake và chạy quy trình sinh content nhiều bước…
  - 🧱 `AdsIntelligencePipeline` L294-350 — Skill dạng pipeline pre-fill intake và thực thi phân tích ads intelligence/đối thủ…
  - 🧱 `CompetitorComparisonSkill` L365-407 — Skill vận hành xây dựng context và prompt để so sánh doanh nghiệp với các đối thủ được chỉ tên…
  - 🧱 `BrandPositioningSkill` L414-462 — Skill vận hành xây dựng context và prompt để sinh một câu định vị brand…
  - 🧱 `AdsOptimizerSkill` L581-621 — Skill vận hành xây dựng context và prompt cho khuyến nghị tối ưu chi tiêu ads…
  - 🧱 `CampaignBriefDynamicSkill` L628-682 — Skill vận hành xây dựng động prompt campaign brief dựa trên intake đã thu thập…
  - 🧱 `ContentCalendarDynamicSkill` L685-763 — Skill vận hành sinh động prompt content calendar, có tính tới pill…
  - 🧱 `AdsCopySkill` L766-860 — Skill vận hành xây dựng context và prompt để sinh các variant ads copy.
  - 🧱 `VideoScriptsSkill` L863-984 — Skill vận hành xây dựng context và prompt để sinh video script dạng ngắn.
  - 🧱 `ViralVideoAnalyzerSkill` L987-1152 — Skill vận hành phân tích transcript/phân tích hình ảnh video viral và resolve chúng…
  - 🔧 `_config_for` L52-74 — Xây dựng một đối tượng OperationalSkillConfig cho một tên task cho trước từ default chung.
  - 🔧 `calc_dynamic_pillar_mix` L117-176 — Tính tỷ lệ phân bổ pillar content cho một content calendar dựa trên business profi…
  - 🔧 `_run_async_sync` L1155-1172 — Helper chạy một coroutine bất đồng bộ tới khi hoàn tất từ code đồng bộ, kết nối sync/as…
  - 🔧 `get_operational_skill` L1211-1216 — Tra cứu factory/registry trả về instance OperationalSkill phù hợp cho một…

### `agents/output_formats.py`
Định nghĩa các template chuỗi định dạng output và hướng dẫn ngôn ngữ dùng để định hướng cấu trúc phản hồi LLM (ví dụ bullet list, bảng) và mức độ địa phương hóa.
  - 🔧 `get_format_instruction` L310-320 — Trả về văn bản hướng dẫn prompt tương ứng với một định dạng output được yêu cầu.

### `agents/pipeline.py`
Module orchestration cốt lõi sắp xếp thứ tự pipeline chiến lược marketing multi-agent — intake, market research, phân tích đối thủ, pricing, SWOT, retention/winback, synthesis, và skill vận hành…
  - 🧱 `StageConfig` L825-834 — Đối tượng config nhỏ mô tả tên, hàm runner, và metadata của một giai đoạn pipeline dùng…
  - 🔧 `run_intake` L189-256 — Chạy luồng hội thoại intake, trích xuất một business profile có cấu trúc từ người dùng…
  - 🔧 `_extract_profile_from_response` L259-306 — Parse văn bản phản hồi intake của LLM thành một đối tượng BusinessProfile có cấu trúc.
  - 🔧 `_run_agent` L313-339 — Helper chung gọi một hàm wrapper agent chiến lược và ghi lại output của nó vào…
  - 🔧 `_run_skill` L342-585 — Helper thực thi skill vận hành chung, xây dựng context/prompt của skill, gọi LLM…
  - 🔧 `_competitor_grounded_pass` L594-622 — Chạy một lượt follow-up có grounding trên giai đoạn phân tích đối thủ để bổ sung dữ liệu đã kiểm chứng…
  - 🔧 `run_retention_strategy_stage` L655-674 — Chạy giai đoạn chiến lược retention của pipeline chiến lược và ghi lại kết quả.
  - 🔧 `run_winback_vision_stage` L677-692 — Chạy giai đoạn win-back vision của pipeline chiến lược và ghi lại kết quả.
  - 🔧 `run_usp_definition` L695-715 — Chạy giai đoạn định nghĩa USP (unique selling proposition) của pipeline chiến lược.
  - 🔧 `run_operational_skill` L740-756 — Entry point tra cứu và thực thi một skill vận hành có tên trên session hiện tại…
  - 🔧 `run_strategic_single_skill` L785-795 — Chạy một giai đoạn chiến lược đơn lẻ độc lập, dùng khi người dùng muốn làm lại hoặc lấy riêng…
  - 🔧 `run_full_pipeline` L852-893 — Chạy toàn bộ pipeline chiến lược end-to-end qua tất cả các giai đoạn theo thứ tự cho một sessi…
  - 🔧 `run_targeted_pipeline` L913-954 — Chạy một tập con các giai đoạn pipeline nhằm vào output do người dùng chọn cụ thể thay vì t…
  - 🔧 `run_multi_agent_targeted` L969-1042 — Điều phối thực thi nhiều giai đoạn agent có mục tiêu, song song/tuần tự dựa trên…

### `agents/prompts.py`
Thư viện lớn các hằng số template prompt system/user tĩnh cho các giai đoạn pipeline chiến lược (intake, market research, đối thủ, khách hàng, pricing, social).
  - 🔧 `get_intake_system` L998-1008 — Trả về template prompt system intake phù hợp dựa trên loại task được yêu cầu.

### `agents/skills.py`
Định nghĩa class cơ sở AgentSkill và các class skill chiến lược cụ thể (market research, đối thủ, customer insight, tâm lý giá, social listening, USP, SWOT, strategy synthesis, tactical pl…
  - 🧱 `AgentSkill` L68-103 — Class cơ sở abstract cho các skill agent chiến lược, định nghĩa hợp đồng build_context/build_user_msg…
  - 🧱 `MarketResearchSkill` L106-126 — Skill chiến lược xây dựng prompt cho giai đoạn market research của pipeline.
  - 🧱 `CompetitorSkill` L129-157 — Skill chiến lược xây dựng prompt cho giai đoạn phân tích đối thủ của pipeline.
  - 🧱 `CustomerInsightSkill` L160-178 — Skill chiến lược xây dựng prompt cho giai đoạn phân tích customer insight của pipeli…
  - 🧱 `PsychologyPricingSkill` L181-210 — Skill chiến lược xây dựng prompt cho giai đoạn khuyến nghị pricing dựa trên tâm lý.
  - 🧱 `SocialListeningSkill` L213-232 — Skill chiến lược xây dựng prompt cho giai đoạn phân tích social listening.
  - 🧱 `UspDefinitionSkill` L235-300 — Skill chiến lược xây dựng prompt để định nghĩa unique selling proposition của doanh nghiệp…
  - 🧱 `StrategySynthesisSkill` L303-343 — Skill chiến lược tổng hợp toàn bộ output các giai đoạn pipeline trước đó thành một chiến lư…
  - 🧱 `SwotSkill` L346-366 — Skill chiến lược xây dựng prompt để sinh phân tích SWOT.
  - 🧱 `TacticalPlaybookSkill` L369-408 — Skill chiến lược xây dựng prompt để sinh một playbook thực thi tactical.

### `agents/strategy_prompts.py`
Xây dựng prompt user strategy-synthesis tổng hợp bằng cách kết hợp output các giai đoạn trước (brief, định dạng brief-thành-block) thành một input LLM duy nhất.
  - 🔧 `build_strategy_user` L179-209 — Lắp ghép prompt user strategy-synthesis đầy đủ từ business profile và toàn bộ output trước…
  - 🔧 `brief_to_block` L212-238 — Định dạng văn bản brief của một giai đoạn thành một block có nhãn để đưa vào prompt synthesis…

### `frameworks/__init__.py`
Marker package cho module frameworks, re-export thư viện KPI, framework SAVE, và helper framework SMART làm điểm vào công khai.

### `frameworks/industry_context.py`
Định nghĩa một thư viện tĩnh context marketing theo ngành cụ thể (search seed, purchase archetype, gợi ý key-message) và các hàm helper để tra cứu, định dạng context đó cho prompt.
  - 🧱 `IndustryContext` L41-51 — Data model biểu diễn các thuộc tính context marketing của một ngành (archetype, …
  - 🔧 `get_industry_context_as_text` L573-602 — Định dạng dữ liệu context của một ngành thành văn bản thuần phù hợp để đưa vào promp…
  - 🔧 `get_full_industry_brief` L605-614 — Trả về văn bản industry brief tổng hợp đầy đủ để dùng làm context cho prompt.
  - 🔧 `suggest_key_message_hint` L617-660 — Gợi ý một key-message hint cho một tổ hợp ngành và purchase archetype cho trước.
  - 🔧 `resolve_archetype` L705-766 — Xác định purchase archetype phù hợp (hoặc kết hợp nhiều archetype) cho một ngành và mô tả doan…
  - 🔧 `list_industries_by_archetype` L769-782 — Liệt kê tất cả các ngành khớp với một purchase archetype cho trước.
  - 🔧 `format_archetype_block` L785-806 — Định dạng chi tiết của một purchase archetype thành một block văn bản để đưa vào prompt.

### `frameworks/kpi_library.py`
Định nghĩa data model KPIFramework và một thư viện tĩnh các bộ KPI theo ngành cụ thể (chỉ số primary/secondary/vanity, benchmark, unit economics) cùng các helper tra cứu, định dạng văn bản.
  - 🧱 `KPIFramework` L10-21 — Data model mô tả KPI framework của một ngành, gồm các chỉ số primary/secondary/vani…
  - 🔧 `get_framework_as_text` L871-907 — Định dạng KPI framework của một ngành thành văn bản thuần để đưa vào prompt LLM.

### `frameworks/save_framework.py`
Triển khai bộ sinh phân tích theo framework marketing SAVE, tạo ra bản phân tích có cấu trúc solution/access/value/education cho một doanh nghiệp và ngành cho trước.
  - 🔧 `generate_save_analysis` L87-127 — Sinh văn bản phân tích SAVE framework cho một doanh nghiệp dựa trên ngành, mô tả,…

### `frameworks/smart_framework.py`
Triển khai các template thiết lập mục tiêu SMART và helper định dạng, điều chỉnh tỷ lệ mục tiêu theo giai đoạn doanh nghiệp để đưa vào prompt chiến lược.
  - 🔧 `format_smart_prompt` L133-169 — Định dạng một block prompt SMART goals đầy đủ cho một ngành, giai đoạn doanh nghiệp, và mục t…

### `campaign_execution.py`
Sinh một kế hoạch thực thi tactical cho một campaign bằng cách phân loại loại mục tiêu, map nó tới một funnel objective, và gọi LLM router để tạo ra một kế hoạch có cấu trúc, kèm fallback xác định…
  - 🔧 `classify_goal_type` L154-165 — Phân loại một chuỗi mục tiêu campaign dạng văn bản tự do thành một category goal-type chuẩn hóa dựa trên từ kh…
  - 🔧 `generate_execution_plan` L173-218 — Xây dựng context prompt LLM từ dữ liệu session/funnel, gọi router_call để sinh một kế hoạch t…
  - 🔧 `_fallback_plan` L221-229 — Tạo ra một kế hoạch thực thi fallback xác định, được khóa theo loại mục tiêu khi sinh bằng LLM thất bại…

### `campaign_ideation.py`
Module agent lớn nhất trong nhóm; vận hành toàn bộ pipeline ideation campaign từ đầu đến cuối -- sinh câu hỏi discovery, tổng hợp các phương án campaign từ nghiên cứu, đề xuất offer package và lever…
  - 🔧 `generate_discovery_questions` L96-134 — Sinh các câu hỏi discovery phù hợp riêng cho ideation campaign bằng cách kết hợp context ngành…
  - 🔧 `_build_industry_levers_context` L137-157 — Lắp ghép KPI framework theo ngành và văn bản context dùng để làm cơ sở (ground) cho prompt ideation…
  - 🔧 `generate_campaign_needs_question` L160-189 — Sinh một câu hỏi follow-up về nhu cầu campaign qua lệnh gọi LLM router, dùng các lever theo ngành…
  - 🔧 `_build_ideation_context` L292-320 — Xây dựng chuỗi context kết hợp business profile và intake còn thiếu, dùng cho prompt ideation…
  - 🔧 `_extract_json` L323-336 — Trích xuất và parse một object JSON từ văn bản output thô của LLM, dung sai với văn xuôi xung qu…
  - 🔧 `extract_campaigns_from_synthesis` L383-481 — Parse kết quả synthesis/tactical playbook trước đó cùng các câu trả lời intake, gọi LLM route…
  - 🔧 `generate_bait_hint` L508-536 — Sinh một gợi ý/teaser marketing 'bait' ngắn qua lệnh gọi LLM router, dựa trên cơ sở lever theo ngành…
  - 🔧 `propose_campaigns` L539-579 — Gọi trực tiếp Anthropic client để đề xuất một tập các phương án campaign, theo dõi usage, v…
  - 🔧 `refine_user_idea` L582-617 — Tinh chỉnh một ý tưởng campaign do người dùng gửi qua lệnh gọi LLM, kiểm tra mức khớp với chiến lược…
  - 🔧 `format_options_card` L620-636 — Định dạng một danh sách phương án campaign thành thẻ văn bản hiển thị trên Telegram.
  - 🔧 `format_refined_card` L639-681 — Định dạng một ý tưởng campaign đã tinh chỉnh, gồm điểm kiểm tra mức khớp chiến lược, thành m…
  - 🔧 `propose_offer_packages` L723-792 — Xây dựng context ngân sách/team và chi tiết campaign, gọi LLM router để đề xuất offer pa…
  - 🔧 `format_packages_card` L795-812 — Định dạng các offer package đã đề xuất cho một campaign thành thẻ văn bản hiển thị trên Telegram.
  - 🔧 `_parse_duration_days` L888-902 — Parse các cụm văn bản thời lượng tự do (ngày/tuần/tháng) thành số ngày kiểu integer bằng regex.
  - 🔧 `propose_offer_levers` L905-962 — Gọi Anthropic client để đề xuất các offer lever cho một campaign dựa trên các tùy chọn offer…
  - 🔧 `format_levers_card` L965-989 — Định dạng các offer lever đã đề xuất cho một campaign thành thẻ văn bản hiển thị trên Telegram.
  - 🔧 `format_dynamic_finalize_form` L999-1041 — Render một form finalize động cho người dùng điền, với các trường được xác định theo se…
  - 🔧 `parse_dynamic_finalize_form` L1044-1103 — Parse các câu trả lời tự do của người dùng cho form finalize động thành các giá trị trường có cấu trúc…
  - 🔧 `merge_to_brief_fields` L1106-1143 — Hợp nhất dữ liệu người dùng nhập vào form finalize với dữ liệu campaign và lever thành các…
  - 🔧 `campaign_to_brief_fields` L1147-1156 — Chuyển đổi trực tiếp một object campaign đã finalize thành các trường brief bằng cách ủy quyền cho merge_to…

### `campaign_intake.py`
Vận hành hội thoại intake campaign: parse tín hiệu hoàn tất, xây dựng một campaign draft từ dữ liệu chiến lược, sinh brand voice, và render thẻ draft/xác nhận.
  - 🔧 `parse_intake_completion` L31-63 — Trích xuất và parse một block JSON hoàn tất từ output lượt intake của LLM, kiểm tra các trường bắt buộc…
  - 🔧 `build_campaign_draft_from_strategy` L70-119 — Xây dựng một campaign draft ban đầu (mục tiêu, wedge, ngân sách, positioning) suy ra từ …
  - 🔧 `generate_brand_voice` L126-181 — Gọi LLM router để sinh một brand voice profile có cấu trúc (tone, quy tắc nên/không nên) …
  - 🔧 `run_intake_turn` L188-235 — Chạy một lượt hội thoại intake đơn lẻ: xây dựng context prompt từ draft/chiến lược/profile…
  - 🔧 `_strategy_to_ctx` L242-271 — Chuyển đổi một object chiến lược thành chuỗi context cô đọng (positioning, wedge, ngân sách, p…
  - 🔧 `render_campaign_draft_card` L278-323 — Render campaign draft đang xây dựng, kèm tóm tắt brand voice, thành thẻ văn bản hiển thị trên Telegram.
  - 🔧 `render_campaign_confirmed_card` L326-337 — Render tóm tắt campaign đã xác nhận cuối cùng thành thẻ văn bản hiển thị trên Telegram.

### `discovery.py`
Vận hành hội thoại business discovery và pipeline nghiên cứu: parse trạng thái hoàn tất, điền các research seed, chạy nghiên cứu deep-dive theo từng topic, lắp ghép kết quả, và sinh/lưu một diagnostic brief…
  - 🔧 `parse_completion` L42-65 — Parse output lượt discovery của LLM để phát hiện trạng thái hoàn tất và trích xuất các trường profi…
  - 🔧 `run_discovery_turn` L68-116 — Chạy một lượt hội thoại discovery đơn lẻ bằng cách gọi LLM router với profile/lịch sử …
  - 🔧 `_fill_seeds` L131-152 — Điền các template research seed bằng giá trị business profile để chuẩn bị nghiên cứu theo từng topic…
  - 🔧 `_track` L155-168 — Ghi lại tiến trình/kết quả nghiên cứu vào object session cho một nhãn topic nghiên cứu cho trước.
  - 🔧 `_run_one_research` L171-230 — Thực thi một tác vụ nghiên cứu deep-dive cho một topic cho trước và ghi lại kết quả qua _track.
  - 🔧 `run_discovery_research` L233-290 — Điều phối toàn bộ tập tác vụ nghiên cứu deep-dive discovery trên các topic, dùng _fil…
  - 🔧 `assemble_research_from_deepdives` L293-330 — Kết hợp các kết quả nghiên cứu deep-dive riêng lẻ thành một context nghiên cứu hợp nhất duy nhất…
  - 🔧 `parse_brief_json` L333-360 — Trích xuất và parse block JSON diagnostic-brief có cấu trúc từ output của LLM.
  - 🔧 `generate_diagnostic_brief` L363-400 — Gọi LLM router với context nghiên cứu đã lắp ghép để sinh diagnostic brief, sau đó…
  - 🔧 `persist_discovery` L403-440 — Lưu các cập nhật discovery profile và diagnostic brief vào lớp session/storage.
  - 🔧 `render_brief_card` L443-485 — Render diagnostic brief thành thẻ văn bản hiển thị trên Telegram.

### `funnel_mapper.py`
Sinh và render một bản đồ funnel marketing (các giai đoạn, content, channel) từ context chiến lược/campaign, hỗ trợ xuất sang định dạng Markdown và Excel (openpyxl).
  - 🔧 `parse_funnel_map` L1-1 — Trích xuất và parse block JSON funnel-map có cấu trúc từ output của LLM.
  - 🔧 `generate_funnel_map` L1-1 — Gọi LLM router để sinh một funnel map có cơ sở từ context chiến lược/campaign, kèm fall…
  - 🔧 `_fallback_funnel_map` L1-1 — Tạo ra một funnel map fallback xác định khi sinh bằng LLM thất bại hoặc trả về không hợp lệ…
  - 🔧 `render_funnel_map_card` L1-1 — Render funnel map thành thẻ văn bản hiển thị trên Telegram.
  - 🔧 `render_funnel_map_summary` L1-1 — Render một view tóm tắt cô đọng của funnel map để hiển thị nhanh.
  - 🔧 `build_funnel_map_markdown` L1-1 — Xây dựng một tài liệu Markdown đầy đủ biểu diễn funnel map để xuất/tải xuống.
  - 🔧 `funnel_map_to_calendar_input` L1-1 — Chuyển đổi cấu trúc funnel map thành định dạng input mà content-calenda… mong đợi.
  - 🔧 `build_funnel_map_excel` L1-1 — Xây dựng một workbook Excel (qua openpyxl) biểu diễn funnel map để xuất/tải xuống.

### `orchestrator.py`
Điều phối pipeline chiến lược multi-agent: chạy các tier agent theo thứ tự/song song kèm xử lý abort, định nghĩa các data class config pipeline, và bao gồm một self-test smoke-test harness…
  - 🔧 `run_tier` L1-1 — Chạy tất cả agent trong một tier pipeline đơn lẻ, xử lý concurrency và lỗi theo từng agent…
  - 🔧 `_safe_run` L1-1 — Bao một lệnh gọi agent đơn lẻ bằng xử lý lỗi để các lỗi không làm sập toàn bộ pipelin…
  - 🔧 `get_strategic_pipeline_tiers` L1-1 — Trả về cấu hình tier theo thứ tự định nghĩa agent nào chạy ở giai đoạn pipeline nào.
  - 🔧 `run_multi_agent_pipeline` L1-1 — Entry point cấp cao nhất chạy toàn bộ pipeline chiến lược multi-agent qua tất cả các cấu hình…
  - 🔧 `format_pipeline_summary` L1-1 — Định dạng kết quả chạy pipeline đã tổng hợp thành một bản tóm tắt dễ đọc.
  - 🔧 `_smoke_test` L1-1 — Self-test/demo harness chạy pipeline với các agent giả (mock) để kiểm chứng quá trình orchestrati…

### `strategy.py`
Sinh chiến lược marketing tổng thể của doanh nghiệp qua lệnh gọi LLM router có cơ sở từ industry brief và business profile, lưu/truy xuất các phiên bản chiến lược, render thẻ chiến lược, và chạy entry point cấp cao nhất của advisor…
  - 🔧 `parse_strategy_json` L1-1 — Trích xuất và parse block JSON chiến lược có cấu trúc từ output của LLM.
  - 🔧 `generate_strategy` L1-1 — Xây dựng context industry-brief và profile, gọi router_call để sinh một chiến lược marketing…
  - 🔧 `persist_strategy` L1-1 — Lưu một chiến lược đã sinh và cập nhật theo dõi engagement qua session adapter/lớp lưu trữ v2…
  - 🔧 `get_existing_strategy` L1-1 — Truy xuất phiên bản chiến lược đã lưu mới nhất cho một người dùng, nếu có, qua storage ada… v2.
  - 🔧 `render_strategy_card` L1-1 — Render chiến lược đã sinh (positioning, wedge, phân bổ ngân sách, kế hoạch theo giai đoạn) thành m…
  - 🔧 `run_advisor` L1-1 — Entry point advisor cấp cao nhất lắp ghép nghiên cứu, sinh diagnostic brief, sinh chiến lư…

### `workflow_runner.py`
Vận hành workflow 'write content': resolve hoặc sinh một brand direction (kèm fallback xác định) và điều khiển thực thi skill viết content cùng các callback tiến trình.
  - 🔧 `_build_brand_direction_fallback` L40-58 — Xây dựng một block văn bản brand-direction fallback xác định khi sinh bằng LLM không khả dụ…
  - 🔧 `_get_brand_direction` L61-129 — Resolve brand direction cho việc viết content -- dùng brand voice đã cache nếu có,…
  - 🔧 `run_write_content_workflow` L132-229 — Chạy toàn bộ workflow write-content: resolve brand direction, gọi skill content vận hành…

### `agents/manager_personas.py`
Định nghĩa data model ManagerPersona và logic định tuyến chọn persona 'manager' marketing nào (và context orchestration) sẽ xử lý tin nhắn của người dùng, cùng các helper render cho phần giới thiệu persona…
  - 🧱 `ManagerPersona` L23-33 — Model dạng dataclass biểu diễn một persona manager marketing, lưu key, tên hiển thị n…
  - 🔧 `get_persona` L526-527 — Tra cứu một ManagerPersona theo key của nó từ registry persona trong bộ nhớ.
  - 🔧 `get_all_personas` L530-531 — Trả về danh sách đầy đủ các persona manager đã đăng ký.
  - 🔧 `route_to_persona` L549-588 — Tính điểm tin nhắn của người dùng theo từ khóa trigger của persona và trạng thái session để quyết định persona nào xử lý…
  - 🔧 `_build_orchestration_suffix` L618-636 — Xây dựng một hậu tố văn bản tóm tắt context orchestration (ví dụ trạng thái handoff giữa các persona)…
  - 🔧 `run_persona_turn` L655-700 — Thực thi một lượt hội thoại cho persona đã chọn, lắp ghép context session, pr…
  - 🔧 `render_team_card` L707-715 — Render một thẻ văn bản đã định dạng liệt kê các persona manager khả dụng để hiển thị trong T…
  - 🔧 `render_persona_intro` L718-726 — Render một tin nhắn giới thiệu cho một persona cho trước, thay đổi nội dung cho người dùng lần đầu v…

### `agents/task_registry.py`
Định nghĩa model TaskConfig và một registry tĩnh các task marketing (ví dụ content calendar, brand voice), gồm yêu cầu trường intake và các helper tra cứu dùng để dẫn dắt việc chọn task của bot…
  - 🧱 `TaskConfig` L17-47 — Model dạng dataclass mô tả một task marketing có thể cấu hình, gồm nhãn, butto…
  - 🔧 `get_task` L559-561 — Tra cứu một TaskConfig theo tên từ registry task tĩnh.
  - 🔧 `list_by_category` L564-566 — Trả về tất cả task đã đăng ký thuộc một category cho trước để duyệt menu/category.
  - 🔧 `needs_intake` L569-584 — Xác định xem một task cho trước có còn cần thông tin intake từ người dùng hay không bằng cách kiểm…

### `agents/campaign_intake_prompts.py`
Xây dựng các chuỗi prompt user LLM cho giai đoạn agent campaign intake và funnel-mapper, lắp ghép context brand-voice, chiến lược, và profile thành văn bản prompt có cấu trúc.
  - 🔧 `build_intake_user` L87-125 — Lắp ghép prompt lượt-user cho agent campaign intake từ context chiến lược, profi…
  - 🔧 `build_funnel_mapper_user` L237-252 — Xây dựng prompt lượt-user cho giai đoạn agent funnel-mapper từ dữ liệu campaign, industr…

### `agents/campaign_scope_library.py`
Thư viện tĩnh các định nghĩa campaign scope theo ngành cụ thể, kèm helper tra cứu và định dạng prompt dùng để làm cơ sở cho các agent lập kế hoạch campaign theo scope ngành cụ thể.
  - 🔧 `get_scope` L134-136 — Tra cứu định nghĩa campaign scope cho một ngành cho trước từ SCOPE_LIBRARY tĩnh…
  - 🔧 `format_scope_for_prompt` L139-151 — Định dạng dữ liệu campaign scope của một ngành thành một block văn bản dễ đọc để đưa vào …

### `agents/discovery_prompts.py`
Xây dựng các chuỗi prompt user LLM cho giai đoạn market-research và sinh brief của pipeline agent discovery.
  - 🔧 `build_research_user` L204-212 — Xây dựng prompt lượt-user cho giai đoạn agent market-research từ context profile, ind…
  - 🔧 `build_brief_user` L215-260 — Xây dựng prompt lượt-user cho giai đoạn agent discovery brief, kết hợp profile, indus…

### `agents/social_industry_profiles.py`
Thư viện tĩnh nội dung social-media theo ngành cụ thể (gồm các dòng content TikTok) kèm helper tra cứu dùng bởi các agent sinh content.
  - 🔧 `get_tiktok_content_lines` L379-385 — Trả về các gợi ý dòng content TikTok cho một ngành cho trước, chuẩn hóa ngành…
  - 🔧 `get_social_industry_profile` L388-395 — Trả về dict profile social-industry đầy đủ cho một ngành cho trước, chuẩn hóa…

### `agents/surgical_edit.py`
Triển khai chỉnh sửa tài liệu có mục tiêu: tách một tài liệu đã sinh thành các section đánh số, phát hiện section nào người dùng nhắm tới qua một lệnh gọi LLM, viết lại đúng các section đó, và lắp ghép lại…
  - 🔧 `split_sections` L28-47 — Tách một tài liệu markdown thành một danh sách các object section đánh số bằng cách dùng tiêu đề section…
  - 🔧 `_sections_outline` L50-52 — Tạo ra một outline văn bản gọn (các tiêu đề đánh số) của các section trong tài liệu để dùng tro…
  - 🔧 `detect_edit_target` L75-113 — Gọi một LLM để xác định section nào của tài liệu mà comment sửa của người dùng đang nhắm tới, p…
  - 🔧 `_rewrite_section` L126-148 — Gọi một LLM để viết lại nội dung một section tài liệu đơn lẻ theo hướng dẫn sửa của người dùng…
  - 🔧 `patch_document` L151-207 — Điều phối toàn bộ luồng surgical-edit: tách tài liệu, phát hiện mục tiêu sửa, viết lại…
  - 🔧 `summarize_changes` L210-220 — Xây dựng một bản tóm tắt dễ đọc liệt kê section nào đã được sửa, dựa trên kết quả phát hiện…

### `agents/task_workflows.py`
Định nghĩa data class WorkflowStep mô tả một bước đơn lẻ (skill, persona, các khóa context bắt buộc) trong một workflow task agent nhiều bước.
  - 🧱 `WorkflowStep` L11-17 — Bản ghi dạng dataclass mô tả một bước workflow: skill, persona, tên persona, các khóa context yêu cầ…

### `frameworks/pricing_segment_library.py`
Thư viện tĩnh các định nghĩa pricing segment theo ngành cụ thể, kèm helper tra cứu và định dạng prompt dùng bởi các agent chiến lược pricing.
  - 🔧 `get_pricing_segments` L161-164 — Tra cứu các định nghĩa pricing segment cho một ngành cho trước từ PRICING_SEG… tĩnh.
  - 🔧 `format_pricing_segments_for_prompt` L167-173 — Định dạng dữ liệu phân khúc giá của một ngành thành khối văn bản dễ đọc để chèn vào prompt.


---

## Web Dashboard & Chat API
_Backend FastAPI/Starlette vận hành web dashboard và trợ lý chat Max: REST endpoint, event streaming, store dựa trên Supabase/SQLite, các view business/account, notification, và đóng gói build standalone…_

### `webapp/api.py`
Định nghĩa toàn bộ bảng route Starlette cho REST/SSE API của web dashboard, bao gồm bootstrap state, cập nhật streaming trực tiếp, các endpoint dữ liệu business (skill run, content, intake, agent run, ads), ch…
  - 🔧 `_ok` L18-19 — Hàm helper trả về response JSON thành công chuẩn, bọc lấy state hiện tại của dashboard.
  - 🔧 `full_state` L22-33 — Xây dựng payload state đầy đủ của dashboard, gộp state của store với dữ liệu Telegram và business…
  - 🔧 `bootstrap` L36-37 — Endpoint API trả về toàn bộ state khởi tạo ban đầu của dashboard cho client bootstrap.
  - 🔧 `stream` L40-61 — Endpoint Server-Sent Events cho client subscribe vào event hub và stream cập nhật dashboard…
  - 🔧 `notify_test` L64-66 — Endpoint debug gửi một thông báo Telegram thử nghiệm để xác nhận bot vẫn kết nối được.
  - 🔧 `biz_data` L70-72 — Endpoint trả về dữ liệu hồ sơ business/chiến lược cho một user cho trước.
  - 🔧 `biz_skillrun` L75-77 — Endpoint chạy một skill sinh content và trả về kết quả, suy biến nhẹ nhàng nếu lỗi…
  - 🔧 `biz_save_profile` L80-84 — Endpoint lưu lại một hồ sơ business đã được chỉnh sửa.
  - 🔧 `biz_intake` L87-91 — Endpoint tiến hội thoại intake business sang lượt tiếp theo.
  - 🔧 `biz_skillrun_rate` L94-98 — Endpoint ghi nhận đánh giá của người dùng cho một skill run đã sinh trước đó.
  - 🔧 `biz_skillrun_save` L101-105 — Endpoint lưu chỉnh sửa của người dùng vào output đã sinh của một skill run.
  - 🔧 `biz_skill_versions` L108-111 — Endpoint liệt kê các phiên bản lịch sử của một skill run cho một user.
  - 🔧 `biz_skillrun_patch` L114-118 — Endpoint áp dụng cập nhật một phần lên bản ghi skill run.
  - 🔧 `biz_agent_run` L121-132 — Endpoint kích hoạt một lần chạy pipeline agent marketing cho một task cho trước và thông báo Teleg…
  - 🔧 `biz_ads` L135-139 — Endpoint trả về dữ liệu hiệu suất ads cho một tài khoản đã liên kết.
  - 🔧 `biz_fb_connect_url` L142-145 — Endpoint trả về URL kết nối Facebook OAuth cho một user.
  - 🔧 `chat` L149-154 — Endpoint xử lý một lượt chat với trợ lý marketing và trả về câu trả lời.
  - 🔧 `chat_history` L157-160 — Endpoint trả về lịch sử chat đã lưu của một user.
  - 🔧 `add_tracked` L164-169 — Endpoint thêm một mục theo dõi mới vào store của dashboard.
  - 🔧 `del_tracked` L171-172 — Endpoint xóa một mục theo dõi khỏi store của dashboard.
  - 🔧 `toggle_job` L176-177 — Endpoint chuyển đổi trạng thái bật/tạm dừng của một scheduled job.
  - 🔧 `apply_optimization` L179-182 — Endpoint áp dụng một gợi ý tối ưu hóa và thông báo Telegram.
  - 🔧 `dismiss_alert` L184-185 — Endpoint bỏ qua một alert đang hoạt động trên dashboard.
  - 🔧 `set_setting` L187-192 — Endpoint cập nhật một cặp khóa/giá trị setting của dashboard.
  - 🔧 `add_campaign` L196-203 — Endpoint tạo một campaign marketing mới và thông báo Telegram.
  - 🔧 `del_campaign` L205-206 — Endpoint xóa một campaign marketing.
  - 🔧 `add_calendar_post` L210-216 — Endpoint thêm một mục bài đăng mới vào content calendar.
  - 🔧 `del_calendar_post` L218-219 — Endpoint xóa một mục bài đăng khỏi content calendar.
  - 🔧 `generate_content` L223-226 — Endpoint kích hoạt sinh các mục content mới cho một chủ đề.
  - 🔧 `add_report` L230-235 — Endpoint tạo một bản ghi report mới.
  - 🔧 `del_report` L237-238 — Endpoint xóa một bản ghi report.
  - 🔧 `connect_account` L242-249 — Endpoint kết nối một tài khoản ad/social mới và thông báo Telegram.
  - 🔧 `toggle_account` L251-252 — Endpoint chuyển đổi trạng thái hoạt động của một tài khoản đã kết nối.
  - 🔧 `disconnect_account` L254-255 — Endpoint gỡ kết nối một tài khoản đã liên kết.
  - 🔧 `set_quota` L259-261 — Endpoint đặt quota sử dụng của một user về một giá trị cụ thể.
  - 🔧 `add_quota` L263-265 — Endpoint tăng quota sử dụng của một user theo một lượng tương đối.
  - 🔧 `reset_usage` L267-268 — Endpoint reset bộ đếm usage của một user về 0.
  - 🔧 `api_routes` L271-308 — Lắp ráp và trả về danh sách Route Starlette đầy đủ, ánh xạ các URL path tới tất cả endpoint…

### `webapp/__init__.py`
File đánh dấu package rỗng cho package webapp.

### `webapp/events.py`
Triển khai event Hub theo mô hình pub/sub dùng cho streaming Server-Sent Events, cùng các background task định kỳ poll một hàm snapshot state và lắng nghe thay đổi realtime từ Supabase để đẩy cập nhật dashboard trực tiếp…
  - 🧱 `Hub` L19-54 — Hub publish/subscribe in-memory quản lý các queue asyncio theo từng client, khử trùng lặp các sự kiện giống nhau…
  - 🔧 `watcher` L60-69 — Background task định kỳ poll một hàm snapshot và publish các thay đổi vào …
  - 🔧 `realtime_listener` L80-108 — Background task subscribe vào thông báo thay đổi realtime của Supabase Postgres và pu…

### `webapp/store.py`
Module facade chọn giữa storage backend SQLite hoặc Supabase khi runtime dựa trên cấu hình environment, và chuyển tiếp tất cả các thao tác dữ liệu dashboard (tracked item, job, campaign, cale…
  - 🔧 `configure` L20-36 — Chọn storage backend đang hoạt động (SQLite theo mặc định, Supabase nếu có SUPABASE_URL/SUPABASE_…
  - 🔧 `backend_name` L39-40 — Trả về tên của storage backend đang hoạt động hiện tại.
  - 🔧 `init` L44-44 — Khởi tạo storage backend đang hoạt động (tạo bảng/seed demo data nếu cần).
  - 🔧 `get_state` L45-45 — Lấy về toàn bộ state hiện tại của dashboard từ backend đang hoạt động.
  - 🔧 `add_tracked` L46-46 — Thêm một mục theo dõi qua backend đang hoạt động.
  - 🔧 `del_tracked` L47-47 — Xóa một mục theo dõi qua backend đang hoạt động.
  - 🔧 `toggle_job` L48-48 — Chuyển đổi trạng thái của một scheduled job qua backend đang hoạt động.
  - 🔧 `remove_optimization` L49-49 — Xóa một gợi ý tối ưu hóa qua backend đang hoạt động.
  - 🔧 `dismiss_alert` L50-50 — Bỏ qua một alert qua backend đang hoạt động.
  - 🔧 `set_setting` L51-51 — Đặt một cặp khóa/giá trị setting của dashboard qua backend đang hoạt động.
  - 🔧 `add_campaign` L52-52 — Tạo một campaign marketing qua backend đang hoạt động.
  - 🔧 `del_campaign` L53-53 — Xóa một campaign marketing qua backend đang hoạt động.
  - 🔧 `add_calendar_post` L54-54 — Thêm một bài đăng content-calendar qua backend đang hoạt động.
  - 🔧 `del_calendar_post` L55-55 — Xóa một bài đăng content-calendar qua backend đang hoạt động.
  - 🔧 `generate_content` L56-56 — Sinh các mục content mới cho một chủ đề qua backend đang hoạt động.
  - 🔧 `add_report` L57-57 — Tạo một bản ghi report qua backend đang hoạt động.
  - 🔧 `del_report` L58-58 — Xóa một bản ghi report qua backend đang hoạt động.
  - 🔧 `connect_account` L59-59 — Kết nối một tài khoản ad/social mới qua backend đang hoạt động.
  - 🔧 `toggle_account` L60-60 — Chuyển đổi trạng thái hoạt động của một tài khoản đã kết nối qua backend đang hoạt động.
  - 🔧 `disconnect_account` L61-61 — Gỡ kết nối một tài khoản đã kết nối qua backend đang hoạt động.
  - 🔧 `set_quota` L62-62 — Đặt quota sử dụng của một user qua backend đang hoạt động.
  - 🔧 `add_quota` L63-63 — Tăng quota sử dụng của một user qua backend đang hoạt động.
  - 🔧 `reset_usage` L64-64 — Reset bộ đếm usage của một user qua backend đang hoạt động.

### `webapp/store_supabase.py`
Triển khai dựa trên Supabase REST của interface storage dashboard (tương đương store_sqlite), xử lý tạo client, seed demo data, và các thao tác CRUD cho tracked item, job, campaign, cal…
  - 🔧 `_c` L19-25 — Khởi tạo lười và cache AsyncClient của Supabase dùng SUPABASE_URL/SUPABASE_SERVICE_KE…
  - 🔧 `_empty` L28-31 — Kiểm tra xem một bảng Supabase cho trước hiện có 0 dòng hay không, dùng để quyết định có nên …
  - 🔧 `init` L34-69 — Khởi tạo backend Supabase bằng cách seed demo data cho tất cả bảng `web_` khi chúng còn …
  - 🔧 `get_state` L72-92 — Truy vấn và lắp ráp toàn bộ state của dashboard bằng cách query tất cả các bảng Supabase liên quan.
  - 🔧 `_ins` L96-97 — Hàm helper chèn một dòng vào bảng Supabase và trả về state dashboard đã được làm mới.
  - 🔧 `_del` L99-100 — Hàm helper xóa một dòng theo id khỏi bảng Supabase và trả về state dashboard…
  - 🔧 `add_tracked` L103-104 — Chèn một dòng tracked item vào bảng web_tracked của Supabase.
  - 🔧 `del_tracked` L106-107 — Xóa một dòng tracked item khỏi Supabase.
  - 🔧 `toggle_job` L109-115 — Đọc và đảo trạng thái của một job trong bảng web_jobs của Supabase.
  - 🔧 `remove_optimization` L117-118 — Xóa một dòng optimization khỏi Supabase.
  - 🔧 `dismiss_alert` L120-121 — Xóa một dòng alert khỏi Supabase.
  - 🔧 `set_setting` L123-126 — Upsert một dòng setting khóa/giá trị vào bảng web_settings của Supabase.
  - 🔧 `add_campaign` L128-129 — Chèn một dòng campaign vào Supabase.
  - 🔧 `del_campaign` L131-132 — Xóa một dòng campaign khỏi Supabase.
  - 🔧 `add_calendar_post` L134-135 — Chèn một dòng bài đăng content-calendar vào Supabase.
  - 🔧 `del_calendar_post` L137-138 — Xóa một dòng bài đăng content-calendar khỏi Supabase.
  - 🔧 `generate_content` L140-150 — Sinh lại các mục content cho một chủ đề bằng cách xóa và chèn lại các dòng vào bảng web_cont…
  - 🔧 `add_report` L152-153 — Chèn một dòng report mới vào Supabase, gắn dấu thời gian ngày hôm nay.
  - 🔧 `del_report` L155-156 — Xóa một dòng report khỏi Supabase.
  - 🔧 `connect_account` L158-160 — Chèn một dòng tài khoản đã kết nối mới vào Supabase với trường id sinh ngẫu nhiên.
  - 🔧 `toggle_account` L162-168 — Đọc và đảo trường trạng thái của một tài khoản đã kết nối trong Supabase.
  - 🔧 `disconnect_account` L170-171 — Xóa một dòng tài khoản đã kết nối khỏi Supabase.
  - 🔧 `set_quota` L173-174 — Đặt trực tiếp trường quota của một user trong bảng web_users của Supabase.
  - 🔧 `add_quota` L176-181 — Đọc quota hiện tại và tăng nó theo một giá trị tương đối trong bảng web_users của Supabase.
  - 🔧 `reset_usage` L183-184 — Reset bộ đếm usage của một user về 0 trong bảng web_users của Supabase.

### `webapp/business.py`
Tầng dữ liệu business thật cho web dashboard: đọc các bảng Supabase trực tiếp dùng chung với bot Telegram (profile, campaign, đối thủ, brand voice, skill run) và kích hoạt các pipeline phân tích/skill thật…
  - 🔧 `available` L51-54 — Kiểm tra xem các credential Supabase đã được cấu hình chưa, dùng để chặn/cho phép các tính năng dữ liệu business thật…
  - 🔧 `ensure_client` L57-73 — Khởi tạo lười client Supabase async dùng chung được cả storage và agents module sử dụng…
  - 🔧 `jobs_public` L1-1 — Trả về một view đã rút gọn, an toàn để công khai của các job AI agent in-memory dùng hiển thị trên dashboa…
  - 🔧 `list_users` L1-1 — Liệt kê các user bot đã biết từ Supabase cho bộ chọn user của web dashboard.
  - 🔧 `biz_data` L1-1 — Tổng hợp dữ liệu hồ sơ business, campaign, và skill run thật từ Supabase cho dash…
  - 🔧 `skill_run_content` L1-1 — Lấy về nội dung/output của một skill run cụ thể để hiển thị trên dashboard.
  - 🔧 `save_profile` L1-1 — Lưu các chỉnh sửa đối với hồ sơ business được thực hiện qua web dashboard trở lại Supabase.
  - 🔧 `intake_turn` L1-1 — Tiến hội thoại discovery/intake của business sang lượt tiếp theo, dùng lại logic discov… của bot.
  - 🔧 `rate_skill_run` L1-1 — Ghi nhận đánh giá của người dùng cho một output skill run đã hoàn tất.
  - 🔧 `save_skill_edit` L1-1 — Lưu chỉnh sửa của người dùng vào content đã sinh của một skill run, tạo ra một phiên bản content mới.
  - 🔧 `list_skill_versions` L1-1 — Liệt kê các phiên bản content lịch sử cho một skill run cho trước.
  - 🔧 `patch_skill_run` L1-1 — Áp dụng cập nhật một phần lên bản ghi skill run trong Supabase.
  - 🔧 `ads_data` L1-1 — Lấy về dữ liệu phân tích ads/campaign cho view ads analytics của dashboard.
  - 🔧 `fb_connect_url` L1-1 — Xây dựng URL kết nối Facebook OAuth dùng để liên kết tài khoản ad từ dashboard.
  - 🔧 `run_agent` L1-1 — Kích hoạt một pipeline phân tích/skill marketing thật (ví dụ market research, SWOT, chiến lược …

### `webapp/chat.py`
Triển khai Max, chatbot tư vấn marketing AI của web dashboard, bao gồm lưu trữ lịch sử hội thoại, gợi ý theo từng giai đoạn, và xử lý lượt chat có thể kích hoạt các skill phân tích thật.
  - 🔧 `history` L37-45 — Trả về lịch sử chat đã cache in-memory cho một user, dùng như một fallback đồng bộ nhanh.
  - 🔧 `load_history` L1-1 — Tải lịch sử chat đã lưu của một user từ Supabase, fallback về cache in-memory nếu cần…
  - 🔧 `_advisor_reply` L1-1 — Sinh câu trả lời tư vấn của Max cho một tin nhắn chat của user, dùng LLM router và dữ liệu busines…
  - 🔧 `chat_turn` L1-1 — Xử lý một lượt hội thoại chat của Max: lưu tin nhắn của user, sinh câu trả lời…

### `webapp/notify.py`
Cầu nối best-effort gửi thông báo Telegram cho các hành động thực hiện trên web dashboard, dùng trực tiếp Telegram Bot API qua httpx.
  - 🔧 `notify` L27-45 — Gửi một tin nhắn thông báo tới chat Telegram đã cấu hình, trả về liệu nó…

### `webapp/store_sqlite.py`
Tầng storage mặc định dựa trên SQLite cho state mock/demo của web dashboard (đối thủ theo dõi, job, campaign, account, alert), dùng khi không cấu hình credential Supabase.
  - 🔧 `seed_demo_enabled` L16-18 — Kiểm tra cờ environment WEB_SEED_DEMO để quyết định có nên seed demo data hay không…
  - 🔧 `init` L1-1 — Khởi tạo schema database SQLite và tùy chọn seed demo data ở lần chạy đầu tiên.
  - 🔧 `get_state` L1-1 — Đọc và trả về toàn bộ state dashboard được lắp ráp từ tất cả các bảng SQLite, để renderin…
  - 🔧 `add_tracked` L1-1 — Thêm một bản ghi đối thủ theo dõi mới vào store SQLite và trả về state đã cập nhật.
  - 🔧 `toggle_job` L1-1 — Chuyển đổi trạng thái bật/tắt của một scheduled job trong store SQLite.
  - 🔧 `add_campaign` L1-1 — Chèn một bản ghi campaign mới vào store SQLite và trả về state đã cập nhật.
  - 🔧 `add_calendar_post` L1-1 — Chèn một bản ghi bài đăng content calendar mới vào store SQLite.
  - 🔧 `generate_content` L1-1 — Ghi nhận các mục content đã sinh vào store SQLite cho view content generator.
  - 🔧 `connect_account` L1-1 — Thêm một bản ghi tài khoản ad đã kết nối vào store SQLite.
  - 🔧 `set_quota` L1-1 — Đặt giá trị quota token usage được lưu trong bảng settings của SQLite.
  - 🔧 `reset_usage` L1-1 — Reset bộ đếm token usage đã ghi nhận trong store SQLite về 0.

### `webapp/build_standalone.py`
Script build nhúng CSS và JS của web dashboard vào một file HTML standalone duy nhất để hosting tĩnh (ví dụ GitHub Pages).
  - 🔧 `build` L16-32 — Đọc HTML shell của dashboard cùng các asset CSS/JS và nhúng chúng vào một file tự chứa duy nhất…
  - 🔧 `main` L35-39 — Điểm vào CLI build HTML dashboard standalone và viết nó vào cả web/ và …


---

## Static Web Frontend
_Client dashboard HTML/CSS/JS tĩnh (bản standalone và bản hosted) hiển thị dữ liệu campaign, content, ads được webapp API phục vụ._

### `index.html`
Trang HTML tĩnh lớn, có khả năng là markup front-end của web dashboard, đóng gói phần lớn cấu trúc UI và script/style inline cho giao diện web của marketing OS.

### `web/dashboard-standalone.html`
Phiên bản đóng gói một file duy nhất của dashboard Marketing OS (HTML + CSS inline + JS inline) dùng cho mục đích offline hoặc hosting tĩnh (ví dụ GitHub Pages) mà không cần backend server.

### `web/index.html`
App shell cho SPA dashboard Marketing OS: định nghĩa khung layout sidebar/main/rail, tải Chart.js, data.js, và app.js, để JavaScript render các trang điều hướng theo hash vào #view.

### `web/styles.css`
Stylesheet toàn cục cho SPA dashboard định nghĩa design system theo dark theme (CSS custom property cho màu, bo góc, shadow) và các style layout/component dùng bởi index.html.

### `web/.nojekyll`
File đánh dấu rỗng tắt xử lý Jekyll của GitHub Pages cho web dashboard tĩnh.

### `web/app.js`
Logic ứng dụng single-page client-side cho web dashboard Marketing OS: routing theo hash, render chart, lấy dữ liệu business, AI chat với Max, và render strategy/calendar/campaig…

### `web/data.js`
Định nghĩa mock data và cấu hình navigation/journey (window.MOCK) dùng để nạp dữ liệu cho web dashboard trước khi nối dữ liệu backend thật.

**Tài liệu:** `web/README.md`


---

## Storage & Data Model
_Tầng lưu trữ bao gồm các domain model (Session, BusinessProfile, BrandVoice), các repository v2 dựa trên Supabase (brief, campaign, engagement, post, profile, strategy, user), Facebook connection/oauth/ads-sn…_

### `storage/brand_voice.py`
Tầng storage cho hồ sơ brand voice: đọc/viết các bản ghi brand voice có versioning (tone, rule, từ cấm) vào database và hỗ trợ deactivation.
  - 🔧 `_get_client` L18-21 — Khởi tạo lười và cache client database dùng cho các truy vấn brand voice.
  - 🔧 `_row_to_bv` L24-41 — Chuyển đổi một dòng database thô thành instance model BrandVoice, ép kiểu/đặt giá trị mặc định cho các trường…
  - 🔧 `get_brand_voice` L44-67 — Lấy bản ghi brand voice đang hoạt động được cập nhật gần nhất của một user từ database.
  - 🔧 `save_brand_voice` L70-123 — Vô hiệu hóa brand voice active trước đó của người dùng, tính số version tiếp theo, và…
  - 🔧 `has_brand_voice` L126-129 — Kiểm tra xem người dùng có brand voice profile active không rỗng đã lưu hay không.
  - 🔧 `deactivate_brand_voice` L132-147 — Đánh dấu tất cả bản ghi brand voice của người dùng thành inactive trong database.

### `storage/campaign_history.py`
Tầng storage cho các lần chạy campaign trong quá khứ: xây dựng và lưu tóm tắt campaign kèm embedding, hỗ trợ tìm kiếm tương đồng ngữ nghĩa và liệt kê các campaign trước đây.
  - 🔧 `_get_client` L23-26 — Khởi tạo lười và cache database client dùng cho các truy vấn campaign history.
  - 🔧 `_truncate` L29-32 — Cắt một chuỗi text về độ dài ký tự tối đa.
  - 🔧 `_build_summary` L35-65 — Xây dựng tóm tắt ngôn ngữ tự nhiên dạng rút gọn từ kết quả pipeline của một session để lưu trữ…
  - 🔧 `save_campaign_history` L68-121 — Xây dựng tóm tắt campaign, sinh embedding cho nó, và chèn một bản ghi campaign history mới…
  - 🔧 `search_similar_campaigns` L124-164 — Tạo embedding cho chuỗi truy vấn và thực hiện RPC tìm kiếm tương đồng vector trên các campaign…
  - 🔧 `list_campaigns` L167-188 — Liệt kê các campaign trước đây của người dùng theo thứ tự ngày tạo, giới hạn số lượng tối đa.

### `storage/embeddings.py`
Wrapper mỏng quanh OpenAI embeddings API để chuyển text thành vector embedding phục vụ tìm kiếm ngữ nghĩa.
  - 🔧 `_get_client` L15-31 — Khởi tạo lười và cache OpenAI client dùng để sinh embedding.
  - 🔧 `embed_text` L34-59 — Sinh vector embedding cho một chuỗi text qua OpenAI embeddings API.

### `storage/feedback_log.py`
Tầng storage cho feedback người dùng: ghi log sự kiện feedback, lấy feedback gần đây, và xây dựng tóm tắt digest cho báo cáo định kỳ.
  - 🔧 `log_feedback` L16-49 — Chèn một bản ghi feedback log mới cho người dùng vào database.
  - 🔧 `get_recent_feedback` L52-73 — Lấy các bản ghi feedback log gần đây nhất của người dùng.
  - 🔧 `get_digest_summary` L76-136 — Tổng hợp các bản ghi feedback gần đây thành cấu trúc tóm tắt dùng để xây digest feedback định kỳ…

### `storage/session.py`
Tầng persistence cốt lõi cho session: quản lý connection pool DB, chuẩn hóa/migrate định dạng row session từ v1 sang v2, và cung cấp các phép get/save/reset cho session người dùng.
  - 🔧 `init_pool` L20-24 — Khởi tạo connection pool DB bất đồng bộ dùng chung cho tầng lưu trữ session.
  - 🔧 `init_db` L27-34 — Đảm bảo schema/bảng database của sessions đã tồn tại, tạo mới nếu cần.
  - 🔧 `_normalize_results` L37-51 — Chuẩn hóa dict kết quả pipeline đã lưu của một session về cấu trúc versioned thống nhất.
  - 🔧 `_row_to_session` L54-97 — Chuyển một row database thô thành instance model Session, xử lý việc migrate từ v1 sang v2…
  - 🔧 `get_session` L100-139 — Lấy row session của người dùng từ database và chuyển thành model Session, tạo mới…
  - 🔧 `save_session` L142-215 — Lưu trạng thái hiện tại của model Session trở lại database, upsert row và serialize…
  - 🔧 `_clear_v1_row` L218-228 — Xóa các field định dạng v1 cũ khỏi row session trong quá trình migrate lên v2.
  - 🔧 `reset_session` L231-273 — Reset session của người dùng về trạng thái mặc định mới trong database, xóa kết quả pipeline trước đó…

### `storage/tracked_competitors.py`
Tầng storage cho theo dõi đối thủ: thêm/xóa đối thủ đang theo dõi, liệt kê theo người dùng, và tìm các đối thủ đến hạn kiểm tra ads định kỳ.
  - 🔧 `add_tracked` L15-40 — Thêm một đối thủ mới (theo Facebook page) vào danh sách đối thủ đang theo dõi của người dùng trong database…
  - 🔧 `remove_tracked` L43-54 — Xóa một mục đối thủ đang theo dõi của người dùng khỏi database.
  - 🔧 `list_tracked_by_user` L57-72 — Liệt kê tất cả đối thủ hiện đang được một người dùng cho trước theo dõi.
  - 🔧 `get_due_tracked` L75-105 — Tìm các đối thủ đang theo dõi mà lần kiểm tra theo dõi tiếp theo đã đến hạn.
  - 🔧 `update_after_check` L108-123 — Cập nhật timestamp lần kiểm tra cuối và trạng thái của một đối thủ đang theo dõi sau khi chạy theo dõi xong…

### `storage/__init__.py`
Package init cho module storage; re-export các submodule brand voice, campaign history, embeddings, models, và session storage để tiện import.

### `storage/campaign_intelligence.py`
Ghi log sự kiện campaign intelligence (sửa brief, chuyển giai đoạn) vào Supabase phục vụ phân tích downstream về hành vi campaign của người dùng.
  - 🔧 `log_campaign_intelligence` L18-54 — Xây dựng và chèn một bản ghi sự kiện campaign intelligence ghi nhận người dùng, ngành, giai đoạn, …

### `storage/v2/__init__.py`
Barrel package cho tầng storage v2, re-export các hàm CRUD từ các module briefs, campaigns, engagements, posts, profiles, sessions, skill runs, strategies, và users.

### `storage/v2/_client.py`
Cung cấp factory dùng chung để lấy instance Supabase client dùng trong toàn bộ các module truy cập dữ liệu storage/v2.
  - 🔧 `get_client` L7-10 — Trả về instance Supabase client dùng chung cho mọi module storage/v2 để truy cập database…

### `storage/v2/briefs.py`
Các hàm truy cập dữ liệu để tạo và lấy strategy brief, bao gồm lấy brief mới nhất cho một engagement.
  - 🔧 `insert_brief` L16-52 — Chèn một bản ghi strategy brief mới kèm facts, hypotheses, gaps, và metadata sử dụng model…
  - 🔧 `get_brief` L55-71 — Lấy một bản ghi brief theo ID.
  - 🔧 `get_latest_brief` L74-91 — Lấy brief được tạo gần nhất cho một engagement cho trước.

### `storage/v2/campaigns_v2.py`
Các phép CRUD cho campaign marketing, bao gồm tạo mới, lấy, liệt kê theo người dùng/trạng thái, và cập nhật embedding campaign.
  - 🔧 `create_campaign` L16-51 — Tạo một bản ghi campaign mới kèm ngành, mục tiêu, offer lever, lịch trình, và skill liên kết…
  - 🔧 `get_campaign` L54-69 — Lấy một bản ghi campaign theo ID.
  - 🔧 `list_campaigns_v2` L72-88 — Liệt kê các campaign của người dùng, có thể lọc theo trạng thái, sắp theo ngày tạo.
  - 🔧 `update_campaign_embedding` L91-107 — Cập nhật vector embedding và tóm tắt của campaign dùng cho tìm kiếm/truy xuất ngữ nghĩa.

### `storage/v2/engagements.py`
Quản lý bản ghi engagement đại diện cho một session tư vấn marketing của người dùng, bao gồm tạo mới, tìm engagement active/mới nhất, cập nhật, và liệt kê.
  - 🔧 `create_engagement` L20-41 — Tạo một bản ghi engagement mới từ input discovery của người dùng.
  - 🔧 `get_engagement` L44-60 — Lấy một bản ghi engagement theo ID.
  - 🔧 `get_active_engagement` L63-81 — Tìm engagement gần nhất của người dùng chưa hoàn tất hoặc chưa lưu trữ.
  - 🔧 `get_latest_with_strategy` L84-105 — Lấy engagement mới nhất của người dùng đã có strategy liên kết.
  - 🔧 `update_engagement` L108-127 — Cập nhật các field tùy ý trên một bản ghi engagement theo ID.
  - 🔧 `list_engagements` L130-147 — Liệt kê các engagement của người dùng sắp theo ngày tạo, với limit có thể cấu hình.

### `storage/v2/posts.py`
Tầng truy cập dữ liệu cho các bài đăng trong content calendar, hỗ trợ chèn, lấy, cập nhật, liệt kê theo campaign hoặc người dùng, và xóa mềm.
  - 🔧 `insert_post` L17-55 — Upsert một bài đăng content calendar kèm campaign, channel, pillar, funnel stage, và bản chuyển thể…
  - 🔧 `get_post` L58-73 — Lấy một bản ghi post theo post ID.
  - 🔧 `update_post` L76-94 — Cập nhật nội dung và/hoặc field trạng thái của một post.
  - 🔧 `list_posts_by_campaign` L97-115 — Liệt kê các post chưa bị xóa của một campaign, sắp theo tuần và ngày đã lên lịch.
  - 🔧 `list_posts_by_user` L118-140 — Liệt kê các post của người dùng với lọc tùy chọn theo channel và trạng thái, sắp theo độ mới.
  - 🔧 `soft_delete_post` L143-144 — Đánh dấu một post là đã xóa bằng cách gọi update_post với trạng thái deleted.

### `storage/v2/profiles.py`
Các hàm CRUD cho profile marketing của người dùng, hỗ trợ lấy, xóa, và upsert từng phần field.
  - 🔧 `get_profile` L22-38 — Lấy bản ghi profile marketing của người dùng theo user ID.
  - 🔧 `delete_profile` L41-51 — Xóa bản ghi profile marketing của người dùng.
  - 🔧 `upsert_profile` L54-76 — Chèn hoặc cập nhật profile marketing của người dùng với các field tùy ý được cung cấp.

### `storage/v2/sessions_slim.py`
Quản lý trạng thái session hội thoại nhẹ cho bot Telegram, bao gồm tạo session mặc định, upsert, xóa, và theo dõi timestamp tin nhắn cuối.
  - 🔧 `get_session_slim` L22-42 — Lấy session hội thoại nhẹ của người dùng, fallback về session mặc định nếu…
  - 🔧 `_default_session` L45-56 — Xây dựng dict trạng thái session mặc định dùng khi người dùng chưa có session.
  - 🔧 `upsert_session_slim` L59-93 — Chèn hoặc cập nhật trạng thái session hội thoại, bao gồm stage, pending task, intake…
  - 🔧 `delete_session_slim` L96-106 — Xóa bản ghi session hội thoại của người dùng.
  - 🔧 `touch_last_message` L109-123 — Cập nhật timestamp tin nhắn cuối của session người dùng.

### `storage/v2/skill_runs.py`
Ghi nhận và lấy các lần chạy skill AI (output có version, rating, và feedback) dùng bởi pipeline agent marketing.
  - 🔧 `insert_skill_run` L16-63 — Ghi nhận một lần chạy skill AI mới, tự tăng số version cho cặp người dùng/skill…
  - 🔧 `get_latest_skill_run` L66-84 — Lấy lần chạy skill gần nhất cho một người dùng và tên skill cho trước.
  - 🔧 `list_skill_runs` L87-104 — Liệt kê các lần chạy skill của người dùng, có thể lọc theo tên skill, sắp theo version.
  - 🔧 `delete_skill_runs` L107-117 — Xóa toàn bộ bản ghi lần chạy skill của một người dùng cho trước.
  - 🔧 `update_rating` L120-137 — Cập nhật rating của người dùng và text feedback cho một lần chạy skill cụ thể.

### `storage/v2/strategies.py`
Lưu và lấy các tài liệu strategy marketing (positioning, roadmap, budget, KPI) gắn với một engagement, có hỗ trợ versioning và rating.
  - 🔧 `insert_strategy` L17-74 — Chèn một tài liệu strategy mới (positioning, wedge, roadmap, budget, KPI) với tự tăng…
  - 🔧 `get_strategy` L77-93 — Lấy một bản ghi strategy theo ID.
  - 🔧 `get_latest_strategy` L96-113 — Lấy strategy có version mới nhất cho một engagement cho trước.
  - 🔧 `update_rating` L116-133 — Cập nhật rating của người dùng và text feedback cho một version strategy cụ thể.

### `storage/v2/users.py`
Tầng truy cập dữ liệu tài khoản người dùng xử lý upsert profile, xóa mềm, xóa cache ngành, và theo dõi quota/usage token.
  - 🔧 `get_user` L15-32 — Lấy bản ghi người dùng chưa bị xóa theo user ID.
  - 🔧 `upsert_user` L35-65 — Chèn hoặc cập nhật bản ghi người dùng với tên, mức độ tiếng Anh, quota token, plan, cache ngành…
  - 🔧 `soft_delete_user` L68-83 — Đánh dấu một bản ghi người dùng là đã xóa bằng cách set timestamp deleted_at.
  - 🔧 `clear_industry_cached` L86-101 — Xóa phân loại ngành đã cache trên bản ghi người dùng.
  - 🔧 `add_token_usage` L104-127 — Tăng usage token đã ghi nhận của người dùng dựa trên usage hiện tại lấy qua get_user.
  - 🔧 `set_token_quota` L130-147 — Đặt quota token của người dùng, tạo bản ghi người dùng qua upsert_user nếu cần.
  - 🔧 `reset_token_usage` L150-165 — Reset bộ đếm usage token của người dùng về 0.

### `models.py`
Module data-model trung tâm định nghĩa các domain object cốt lõi -- BusinessProfile, BrandVoice, VersionedResult, và Session -- cùng các enum TaskType/PipelineStage. Được import rộng rãi trong toàn bộ codebase…
  - 🧱 `BusinessProfile` L51-179 — Đại diện cho business profile thu thập trong discovery/intake (ngành, giai đoạn, USP, …
  - 🧱 `BrandVoice` L183-244 — Đại diện cho một brand voice profile đã sinh (mô tả tone, quy tắc do/don't, từ cấm/ưu…
  - 🧱 `VersionedResult` L251-270 — Bọc một phần output pipeline kèm số version và timestamp tạo, hỗ…
  - 🧱 `Session` L274-407 — Đại diện cho toàn bộ trạng thái pipeline session theo từng người dùng -- profile, lịch sử intake, các bản…

### `session_v2_adapter.py`
Tầng adapter kết nối model Session/BusinessProfile trong bộ nhớ với backend persistence v2 -- tải một session đầy đủ (user, profile, slim session state, skill run) và lưu trạng thái session trở lại…
  - 🔧 `get_session_v2` L21-94 — Tải các row user, profile, slim session, và skill-run từ backend v2 và lắp ráp thành…
  - 🔧 `reset_session_v2` L97-111 — Reset trạng thái session v2 của người dùng, xóa profile/session đã lưu để bắt đầu lại từ đầu.
  - 🔧 `save_session_v2` L114-200 — Lưu các field profile và state của object Session trong bộ nhớ trở lại storage backend v2…

### `fb_connections.py`
Tầng truy cập dữ liệu cho kết nối tài khoản Facebook Ads, trạng thái OAuth, snapshot ads, và cooldown cảnh báo, dựa trên các bảng Supabase (user_fb_connections, oauth_states, ads_snapshots, ads_alert_cooldown…
  - 🔧 `save_connection`  — Upsert bản ghi kết nối Facebook Ads cho người dùng, lưu token đã mã hóa, tài khoản active…
  - 🔧 `update_active_account`  — Cập nhật id và tên ad account active cho kết nối Facebook đã lưu của người dùng.
  - 🔧 `get_available_accounts`  — Lấy và phân tích cú pháp danh sách ad account Facebook khả dụng đã lưu cho kết nối của người dùng…
  - 🔧 `get_connection`  — Lấy bản ghi kết nối Facebook Ads đã lưu của người dùng từ Supabase.
  - 🔧 `get_all_active_connections`  — Lấy tất cả kết nối Facebook đã bật notification, dùng bởi scheduler để…
  - 🔧 `update_last_pull`  — Cập nhật timestamp lần pull cuối cho bản ghi kết nối Facebook của người dùng.
  - 🔧 `update_token`  — Cập nhật access token đã mã hóa và timestamp hết hạn cho kết nối của người dùng.
  - 🔧 `update_notification_settings`  — Cập nhật các field tùy chọn notification tùy ý cho kết nối Facebook của người dùng qua key…
  - 🔧 `delete_connection`  — Xóa bản ghi kết nối Facebook Ads của người dùng khỏi Supabase.
  - 🔧 `disable_connection`  — Tắt notification cho kết nối Facebook của người dùng, thường gọi khi token bị…
  - 🔧 `save_oauth_state`  — Lưu một token trạng thái OAuth dùng một lần gắn với user id kèm timestamp hết hạn để chống CSRF…
  - 🔧 `consume_oauth_state`  — Xác thực và tiêu thụ (xóa) token trạng thái OAuth, kiểm tra hết hạn trước khi trả về…
  - 🔧 `compute_campaign_metrics`  — Tính các chỉ số hiệu suất campaign suy ra (CTR, CPA, ROAS, v.v.) từ insight Facebook thô…
  - 🔧 `save_snapshot`  — Lưu một snapshot hàng ngày các chỉ số campaign đã tính cho người dùng vào bảng ads_snapshots…
  - 🔧 `_extract_action`  — Trích số lượng action dạng số của một loại cho trước từ dữ liệu insight Facebook thô của campaign…
  - 🔧 `_extract_action_value`  — Trích giá trị tiền tệ của action cho trước từ dữ liệu insight Facebook thô của campaign…
  - 🔧 `get_snapshot`  — Lấy snapshot campaign đã lưu của người dùng vào một ngày cụ thể.
  - 🔧 `get_snapshots_range`  — Lấy các snapshot campaign đã lưu của người dùng trong một khoảng ngày, dùng để so sánh hàng tuần…
  - 🔧 `cleanup_old_snapshots`  — Xóa các bản ghi snapshot ads cũ hơn cửa sổ lưu giữ 90 ngày.
  - 🔧 `check_and_set_cooldown`  — Kiểm tra xem một loại cảnh báo cho campaign có đang trong cửa sổ cooldown hay không, và nếu không, ghi…

### `workflow_errors.py`
Ghi log các lỗi thực thi workflow có cấu trúc (người dùng, loại task, bước, skill, thông báo) vào lưu trữ bền vững để chẩn đoán.
  - 🔧 `log_workflow_error`  — Chèn một bản ghi lỗi workflow ghi nhận người dùng, loại task, chỉ số bước, tên skill, và lỗi…


---

## Integrations, Background Jobs & Tool Belt
_Năng lực cross-cutting dùng bởi agent và bot: service Facebook OAuth/Ads cùng worker nền notifier/scheduler của nó, các job theo dõi đối thủ và feedback-digest, LLM router, tiện ích crypto/token-billing…_

### `tools/fb_ads_library.py`
Bọc Facebook Ads Library API để tìm ads của đối thủ (theo từ khóa hoặc page id), phân giải URL Facebook page về page id, và định dạng kết quả ads phục vụ phân tích LLM downstream.
  - 🔧 `search_competitor_ads` L29-90 — Tìm trong Facebook Ads Library các ads của đối thủ khớp với từ khóa tìm kiếm và quốc gia/r…
  - 🔧 `search_by_page_id` L93-139 — Tìm trong Facebook Ads Library tất cả ads đang active do một page id cụ thể chạy.
  - 🔧 `format_ads_for_analysis` L146-193 — Định dạng kết quả thô từ Ads Library API thành một khối text có cấu trúc phù hợp cho phân tích bằng LLM…
  - 🔧 `is_available` L196-198 — Kiểm tra xem tích hợp Facebook Ads Library đã được cấu hình và có thể dùng được hay không.
  - 🔧 `_extract_fb_candidate` L205-237 — Trích một định danh page Facebook hoặc đoạn URL ứng viên từ input tùy ý của người dùng…
  - 🔧 `resolve_fb_url` L240-316 — Phân giải một URL Facebook hoặc vanity name về page id chuẩn bằng cách gọi Graph API ho…
  - 🔧 `resolve_page_id_from_url` L319-323 — Wrapper tiện lợi trích một ứng viên Facebook từ text và phân giải nó thành page…

### `tools/fb_marketing.py`
Bọc Facebook Marketing API để lấy insight ở cấp account/ad, quản lý campaign và ad set (đổi trạng thái, cập nhật budget), và định dạng dữ liệu hiệu suất phục vụ phân tích bằng LLM và ads opti…
  - 🔧 `get_account_insights` L56-114 — Lấy insight hiệu suất tổng hợp của ad account (spend, ROAS, CPA, v.v.) từ Faceb…
  - 🔧 `get_account_insights_daily` L117-167 — Lấy chi tiết hiệu suất ad account theo từng ngày từ Facebook Marketi…
  - 🔧 `get_active_campaigns` L170-198 — Lấy danh sách các campaign hiện đang active của một ad account từ Facebook Market…
  - 🔧 `_extract_action_value` L205-212 — Trích giá trị chỉ số của một action cụ thể (ví dụ purchases) từ action của Facebook Insights…
  - 🔧 `_hook_hold_rates` L215-226 — Tính tỷ lệ giữ chân hook và hold của video từ chỉ số video của Facebook.
  - 🔧 `format_insights_for_analysis` L229-311 — Định dạng dữ liệu insight cấp account thành khối text có cấu trúc phục vụ phân tích hiệu suất bằng LLM…
  - 🔧 `format_ad_level_for_analysis` L314-368 — Định dạng dữ liệu hiệu suất cấp ad (gồm cả tỷ lệ hook/hold) thành khối text có cấu trúc…
  - 🔧 `is_available` L371-373 — Kiểm tra xem tích hợp Facebook Marketing API đã được cấu hình và có thể dùng được hay không.
  - 🔧 `has_token` L376-378 — Kiểm tra xem có access token Facebook hợp lệ khả dụng cho các lời gọi API hay không.
  - 🔧 `get_campaigns_with_adsets` L385-415 — Lấy các campaign kèm ad set lồng nhau bên trong từ Facebook Marketing API cho…
  - 🔧 `get_ads_by_adset` L418-436 — Lấy tất cả ads thuộc một ad set cụ thể từ Facebook Marketing API.
  - 🔧 `set_object_status` L448-473 — Cập nhật trạng thái active/paused của một object campaign, ad set, hoặc ad Facebook qua Mar…
  - 🔧 `update_budget` L476-508 — Cập nhật budget hàng ngày hoặc trọn đời của một campaign hoặc ad set Facebook qua Marketing A…
  - 🔧 `format_hierarchy_for_optimizer` L511-555 — Định dạng hệ thống phân cấp campaign/ad-set/ad kèm chỉ số hiệu suất thành khối text có cấu trúc…

### `tools/image_gen.py`
Bọc một provider sinh/sửa ảnh AI để sinh ảnh marketing, sửa ảnh hiện có, phân tích style ảnh, và ước tính chi phí sinh ảnh.
  - 🔧 `_get_client` L23-33 — Khởi tạo lười và cache client dùng cho các lời gọi API sinh ảnh.
  - 🔧 `generate_image` L36-78 — Sinh một ảnh marketing mới từ prompt text dùng provider sinh ảnh AI đã cấu hình…
  - 🔧 `edit_image` L81-127 — Sửa một ảnh hiện có theo hướng dẫn text dùng provider sửa ảnh AI…
  - 🔧 `analyze_image_style` L130-165 — Phân tích các thuộc tính style hình ảnh của một ảnh đã upload qua provider vision AI để dùng cho…
  - 🔧 `estimate_cost` L168-184 — Ước tính chi phí tiền tệ cho một yêu cầu sinh hoặc sửa ảnh dựa trên giá của provider…
  - 🔧 `is_available` L187-189 — Kiểm tra xem tích hợp sinh ảnh đã được cấu hình và có thể dùng được hay không.

### `tools/llm_router.py`
LLM router đa provider trừu tượng hóa Anthropic, Gemini, OpenAI, và Perplexity sau một interface call() thống nhất, có chọn provider theo loại task, chuỗi fallback, và báo cáo availability.
  - 🧱 `Provider` L40-53 — Enum các provider LLM được hỗ trợ (Anthropic, Gemini, OpenAI, Perplexity) dùng để chọn…
  - 🧱 `TaskType` L60-93 — Enum các nhóm loại task (ví dụ reasoning, fast-extraction, grounded-search) dùng để map…
  - 🧱 `ProviderUnavailable` L163-165 — Exception được raise khi một provider LLM cụ thể chưa được cấu hình hoặc không thể truy cập cho một yêu…
  - 🧱 `AllProvidersFailedError` L168-170 — Exception được raise khi mọi provider trong chuỗi fallback đều thất bại khi thực hiện một lời gọi.
  - 🔧 `_get_anthropic_client` L180-188 — Khởi tạo lười và cache instance client Anthropic.
  - 🔧 `_call_anthropic_sonnet` L195-220 — Gọi model Anthropic Claude Sonnet với prompt và tham số cho trước.
  - 🔧 `_call_anthropic_haiku` L223-252 — Gọi model Anthropic Claude Haiku với prompt và tham số cho trước, dùng cho các task…
  - 🔧 `_get_gemini_client` L259-274 — Khởi tạo lười và cache instance client Google Gemini.
  - 🔧 `_calc_thinking_budget` L277-296 — Tính thinking budget (số token reasoning) cần cấp cho một lời gọi Gemini dựa trên loại task…
  - 🔧 `_call_gemini_pro` L299-367 — Gọi model Gemini Pro với prompt cho trước, xử lý thinking budget và phản hồi…
  - 🔧 `_patch_homepage_links` L370-413 — Xử lý hậu kỳ phản hồi grounded-search của Gemini để sửa hoặc chèn link trang chủ vào …
  - 🔧 `_call_gemini_pro_grounded` L416-506 — Gọi model Gemini Pro với web-grounded search được bật cho các task cần thông tin cập nhật…
  - 🔧 `_call_gemini_flash` L509-553 — Gọi model Gemini Flash nhanh hơn, rẻ hơn cho các task nhẹ.
  - 🔧 `_get_openai_client` L560-572 — Khởi tạo lười và cache instance client OpenAI.
  - 🔧 `_call_openai_generic` L575-627 — Helper dùng chung gọi một model OpenAI cụ thể với prompt và tham số cho trước…
  - 🔧 `_call_openai_gpt5` L630-635 — Gọi model OpenAI GPT-5 qua helper gọi OpenAI dùng chung.
  - 🔧 `_call_openai_gpt5_mini` L638-643 — Gọi model OpenAI GPT-5 Mini qua helper gọi OpenAI dùng chung.
  - 🔧 `_call_openai_gpt5_nano` L646-651 — Gọi model OpenAI GPT-5 Nano qua helper gọi OpenAI dùng chung.
  - 🔧 `_call_openai_gpt_4_1_mini` L654-659 — Gọi model OpenAI GPT-4.1 Mini qua helper gọi OpenAI dùng chung.
  - 🔧 `_call_openai_gpt4o` L662-666 — Gọi model OpenAI GPT-4o qua helper gọi OpenAI dùng chung.
  - 🔧 `_call_openai_gpt4o_mini` L669-673 — Gọi model OpenAI GPT-4o Mini qua helper gọi OpenAI dùng chung.
  - 🔧 `_call_perplexity_sonar` L676-682 — Gọi model Perplexity Sonar, thường dùng cho các truy vấn nghiên cứu web-grounded.
  - 🔧 `call` L726-819 — Điểm vào chính của LLM router: chọn chuỗi provider dựa trên TaskType, thử lần lượt…
  - 🔧 `_forced_test_provider` L826-837 — Cho phép test/debug ép router dùng một provider cụ thể, bỏ qua lựa chọn thông thường…
  - 🔧 `is_provider_available` L840-857 — Kiểm tra xem API key/credentials của một provider cho trước đã được cấu hình và dùng được chưa.
  - 🔧 `availability_report` L860-866 — Xây dựng báo cáo tổng hợp các LLM provider hiện đang khả dụng/đã cấu hình.

### `tools/video_vision.py`
Trích xuất keyframe từ video người dùng tải lên qua ffmpeg probing/extraction và phân tích chúng bằng AI vision model để tạo bản tóm tắt phân tích hình ảnh phục vụ đánh giá content marketing.
  - 🔧 `is_available` L37-38 — Kiểm tra xem tích hợp phân tích video vision đã được cấu hình và dùng được chưa.
  - 🔧 `availability_report` L41-44 — Xây dựng báo cáo tổng hợp mô tả trạng thái khả dụng của tích hợp video vision…
  - 🔧 `extract_keyframes` L51-82 — Trích xuất một tập keyframe đại diện từ file video tại các mốc thời gian được chọn dùng…
  - 🔧 `_choose_timestamps` L85-107 — Chọn một tập mốc thời gian phân bố đều hoặc có ý nghĩa theo heuristic trong thời lượng video…
  - 🔧 `_probe_duration` L110-125 — Dò tổng thời lượng của file video bằng ffprobe.
  - 🔧 `_extract_one_frame` L128-140 — Trích xuất một khung hình đơn từ video tại mốc thời gian chỉ định bằng ffmpeg.
  - 🔧 `analyze_frames_with_vision` L165-214 — Gửi các keyframe video đã trích xuất tới AI vision model để phân tích (ví dụ chất lượng content,…
  - 🔧 `extract_visual_analysis` L221-249 — Hàm điều phối cấp cao trích xuất keyframe từ video và chạy phân tích vision…

### `workers/feedback_digest.py`
Worker nền định dạng và chạy job digest định kỳ tổng hợp feedback gần đây của người dùng để xem xét.
  - 🔧 `format_digest` L31-62 — Định dạng dữ liệu digest feedback đã tổng hợp thành báo cáo tóm tắt dễ đọc.
  - 🔧 `run_once` L65-89 — Chạy một lượt của worker feedback digest: thu thập dữ liệu digest, định dạng nó, và gửi…

### `workers/monitor_competitors.py`
Worker nền định kỳ kiểm tra hoạt động ads Facebook của các đối thủ đang theo dõi qua Ads Library và báo cho bot/người dùng khi phát hiện ads mới.
  - 🔧 `_ad_ids_from_response` L33-34 — Trích xuất tập ad id có trong phản hồi của Facebook Ads Library API.
  - 🔧 `check_one` L37-89 — Kiểm tra một đối thủ đang theo dõi xem có hoạt động ads mới nào kể từ lần kiểm tra trước, so sánh…
  - 🔧 `run_once_with_bot` L92-106 — Chạy một lượt theo dõi trên toàn bộ các đối thủ đến hạn kiểm tra và gửi thông báo Telegram…
  - 🔧 `start_background_monitor` L109-121 — Khởi động task nền lặp định kỳ kích hoạt việc kiểm tra theo dõi đối thủ…
  - 🔧 `run_once` L124-141 — Chạy một lượt theo dõi trên toàn bộ các đối thủ đến hạn kiểm tra mà không cần bot…

### `tools/crypto.py`
Cung cấp helper mã hóa/giải mã đối xứng dựa trên Fernet để lưu trữ an toàn các token nhạy cảm (ví dụ token OAuth Facebook) ở trạng thái nghỉ.
  - 🔧 `_get_fernet` L15-23 — Khởi tạo lười và cache một instance cipher Fernet từ encryption key đã cấu hình.
  - 🔧 `encrypt_token` L26-28 — Mã hóa một chuỗi token dạng plaintext bằng mã hóa đối xứng Fernet.
  - 🔧 `decrypt_token` L31-37 — Giải mã một chuỗi token đã được mã hóa Fernet trước đó về lại plaintext.

### `services/fb_oauth.py`
Implement luồng OAuth Facebook: xây dựng authorization URL, trao đổi/gia hạn access token, lấy danh sách ad account, xử lý callback OAuth, và báo cho người dùng qua Telegram khi kết nối xong.
  - 🔧 `build_oauth_url` L45-57 — Xây dựng authorization URL OAuth Facebook cho một người dùng cho trước, lưu lại một state ngẫu nhiên…
  - 🔧 `_exchange_short_token` L62-78 — Đổi authorization code OAuth lấy access token Facebook ngắn hạn qua Graph…
  - 🔧 `_extend_to_long_token` L81-99 — Đổi access token Facebook ngắn hạn lấy token dài hạn và tính thời điểm hết hạn của nó…
  - 🔧 `_get_ad_accounts` L102-114 — Lấy danh sách ad account Facebook mà người dùng đã xác thực có thể truy cập qua Graph…
  - 🔧 `refresh_token_if_needed` L119-146 — Kiểm tra xem token Facebook đã lưu của người dùng có gần hết hạn không và gia hạn/làm mới nếu …
  - 🔧 `_norm_id` L154-156 — Chuẩn hóa id ad account Facebook, bỏ tiền tố 'act_' để so sánh nhất quán…
  - 🔧 `handle_callback` L161-220 — Handler callback kiểu FastAPI cho redirect OAuth Facebook: kiểm tra state, trao đổi…
  - 🔧 `_ask_account_selection` L223-247 — Gửi tin nhắn Telegram kèm nút inline cho phép người dùng chọn ad account Facebook nào…
  - 🔧 `_notify_connected` L250-279 — Gửi tin nhắn xác nhận qua Telegram cho người dùng khi một ad account Facebook đã được kết nối thành…

### `__init__.py`
Marker package rỗng cho module services; không có nội dung thực thi.

### `ads_notifier.py`
Xây dựng và gửi digest/cảnh báo hiệu suất Facebook Ads: kéo/snapshot metric ad account, tính delta, định dạng báo cáo daily/weekly/quick, phát hiện cảnh báo theo ngưỡng, và gửi Telegram an toàn…
  - 🔧 `pull_and_snapshot`  — Giải mã access token của một connection, lấy insight ad account Facebook hiện tại, và lưu…
  - 🔧 `backfill_snapshots`  — Backfill các snapshot insight ads hàng ngày trong lịch sử cho một connection qua một số lượng…
  - 🔧 `compute_delta`  — Tính delta metric tổng hợp và trung bình giữa campaign hôm nay và kỳ trước…
  - 🔧 `format_daily_digest`  — Định dạng tin nhắn digest hiệu suất hàng ngày dễ đọc, gồm sức khỏe account, delta…
  - 🔧 `format_weekly_digest`  — Định dạng digest hiệu suất hàng tuần so sánh số liệu tổng hợp tuần này và tuần trước…
  - 🔧 `fetch_quick_report`  — Lấy và tổng hợp dữ liệu hiệu suất ads cho báo cáo nhanh theo yêu cầu trong một khoảng…
  - 🔧 `format_quick_report`  — Định dạng tin nhắn báo cáo nhanh theo yêu cầu, so sánh metric kỳ hiện tại với kỳ…
  - 🔧 `ads_table_from_metrics`  — Xây dựng bảng định dạng các dòng hiệu suất ads riêng lẻ từ metric theo dõi để đưa vào…
  - 🔧 `_find_alerts`  — Quét các campaign để tìm điều kiện hiệu suất (ví dụ CPA cao, CTR thấp) và tạo danh sách…
  - 🔧 `check_alerts`  — Đánh giá campaign dựa trên metric hiện tại và trước đó để xác định cảnh báo nào nên…
  - 🔧 `format_alert`  — Định dạng một cảnh báo đã kích hoạt thành tin nhắn Telegram dễ đọc.
  - 🔧 `_find_winner`  — Xác định campaign hiệu suất tốt nhất trong một tập dựa trên metric được chọn, dùng để làm nổi…
  - 🔧 `send_message_safe`  — Gửi tin nhắn Telegram có định dạng markdown, fallback về plain text và ghi log…

### `ads_scheduler.py`
Scheduler nền định kỳ kích hoạt digest ads daily/weekly, theo dõi cảnh báo, làm mới token, backfill snapshot, và job dọn dẹp dựa trên kiểm tra thời điểm trong ngày.
  - 🔧 `start_ads_scheduler`  — Khởi động loop scheduler ads nền, định kỳ tick để kích hoạt digest đã lên lịch…
  - 🔧 `_tick`  — Kiểm tra thời gian hiện tại so với khung giờ kích hoạt đã lên lịch và dispatch task async cho…
  - 🔧 `_run_daily_digest`  — Chạy job digest hàng ngày cho tất cả connection ads đang hoạt động, kéo snapshot mới, tính…
  - 🔧 `_run_weekly_report`  — Chạy job báo cáo hàng tuần cho tất cả connection đang hoạt động, lấy khoảng snapshot và gửi…
  - 🔧 `_run_alert_monitor`  — Chạy job theo dõi cảnh báo, kéo snapshot mới, kiểm tra điều kiện cảnh báo, và gửi…
  - 🔧 `_run_token_refresh`  — Làm mới access token Facebook cho tất cả connection đang hoạt động gần hết hạn, xử lý việc bị thu hồi…
  - 🔧 `_run_snapshot_backfill`  — Chạy backfill một lần các snapshot lịch sử cho tất cả connection đang hoạt động lúc scheduler…
  - 🔧 `_run_cleanup`  — Kích hoạt dọn dẹp các bản ghi snapshot ads cũ vượt ngoài khung lưu giữ.
  - 🔧 `_handle_token_revoked`  — Tắt một connection có token Facebook đã bị thu hồi và báo cho người dùng qua Telegram.

### `__init__.py`
Marker package rỗng cho module tools; không có nội dung thực thi.

### `image_prompt_library.py`
Thư viện template prompt sinh ảnh có thể tái sử dụng, phân loại theo loại sản phẩm, kèm helper để phân loại văn bản đầu vào, chọn template phù hợp, và xây dựng prompt ảnh hoàn chỉnh đã định dạng.
  - 🧱 `PromptTemplate`  — Cấu trúc dữ liệu biểu diễn một template prompt ảnh đơn với slug, title, category, tỉ lệ khung hình…
  - 🔧 `categorize`  — Xác định danh mục sản phẩm khớp nhất cho một đoạn văn bản bằng cách tính điểm theo từ khóa trùng…
  - 🔧 `pick_template`  — Chọn template prompt phù hợp nhất cho danh mục, tỉ lệ khung hình, và nội dung…
  - 🔧 `build_prompt`  — Xây dựng chuỗi prompt sinh ảnh hoàn chỉnh bằng cách chọn một template và điền các placeholder…

### `krillin_client.py`
Client trích xuất transcript audio/video qua công cụ CLI Krillin cục bộ hoặc fallback OpenAI Whisper API, xử lý tải file từ xa, kiểm tra khả dụng, và định dạng transcript sẵn sàng cho prompt.
  - 🔧 `extract_transcript`  — Trích xuất transcript từ URL hoặc path nguồn media, ưu tiên dùng Krillin CLI cục bộ…
  - 🔧 `is_available`  — Kiểm tra xem binary Krillin CLI cục bộ đã được cài và có sẵn trên system path chưa.
  - 🔧 `availability_report`  — Xây dựng báo cáo dễ đọc mô tả xem Krillin CLI và fallback Whisper API…
  - 🔧 `_run_krillin`  — Chạy Krillin CLI cục bộ dưới dạng subprocess để chuyển media nguồn thành transcript, phân tích cú pháp JSON…
  - 🔧 `_run_whisper_api`  — Gửi một file audio cục bộ tới OpenAI Whisper API để chuyển thành văn bản và trả về kết quả…
  - 🔧 `_ensure_local_file`  — Tải URL media từ xa về file tạm cục bộ nếu cần, trả về đường dẫn cục bộ…
  - 🔧 `format_transcript_for_prompt`  — Định dạng các đoạn transcript đã trích xuất thành khối văn bản giới hạn độ dài, sẵn sàng cho prompt để sử…

### `workers/__init__.py`
Marker package cho module workers, nơi chứa các task lên lịch kiểu cron chạy nền.

### `tools/token_billing.py`
Tính chi phí sử dụng LLM bằng USD và VND trên các provider và định dạng báo cáo tóm tắt chi phí từ token log.
  - 🔧 `calc_cost_usd` L48-73 — Tính chi phí USD cho một lần gọi LLM dựa trên bảng giá provider và số lượng token (đầu vào…
  - 🔧 `calc_cost_vnd` L76-84 — Chuyển đổi chi phí USD của một lần gọi LLM sang VND để hiển thị cho người dùng Việt Nam.
  - 🔧 `fmt_usd` L87-93 — Định dạng một số tiền USD thành chuỗi tiền tệ dễ đọc.
  - 🔧 `fmt_vnd` L96-98 — Định dạng một số tiền VND thành chuỗi tiền tệ dễ đọc.
  - 🔧 `accumulate_cost` L105-119 — Thêm một khoản chi phí tăng thêm vào tổng chi phí lũy kế lưu trong preferences của session.
  - 🔧 `get_total_cost_usd` L122-130 — Đọc tổng chi phí USD lũy kế từ preferences của session.
  - 🔧 `cost_summary_from_log` L137-183 — Tổng hợp token usage log thành các bản tóm tắt chi phí chia theo provider và theo skill.
  - 🔧 `format_cost_report` L186-213 — Xây dựng báo cáo văn bản đã định dạng về chi phí sử dụng LLM cho một session, chia theo provider…

### `tools/token_tracker.py`
Theo dõi token usage và quota LLM theo từng job và từng skill, lưu trữ usage log và sinh bản tóm tắt/footer usage hiển thị cho người dùng.
  - 🔧 `begin_job` L20-38 — Bắt đầu một chuỗi job được theo dõi mới cho session, tăng bộ đếm job dùng để nhóm…
  - 🔧 `track_skill` L59-121 — Ghi nhận token usage và chi phí tính toán cho một lần chạy skill có tên, thêm một entry vào…
  - 🔧 `track_usage` L124-180 — Trích xuất metadata token usage từ object phản hồi LLM và ghi nhận vào…
  - 🔧 `track_usage_raw` L183-222 — Ghi nhận token usage từ số lượng token input/output thô (dùng khi không có object phản hồi LLM…
  - 🔧 `get_token_log` L225-229 — Trả về danh sách các entry token usage log đã ghi nhận cho một session.
  - 🔧 `get_latest_skill_entry` L232-237 — Tìm entry token log gần nhất khớp với một tên skill cho trước.
  - 🔧 `get_quota` L240-246 — Đọc token quota đã cấu hình cho một session.
  - 🔧 `get_used` L249-255 — Đọc tổng số token đã dùng tính đến hiện tại cho một session.
  - 🔧 `get_remaining` L258-260 — Tính token quota còn lại cho một session bằng quota trừ số token đã dùng.
  - 🔧 `is_low` L263-268 — Kiểm tra xem token quota còn lại đã giảm dưới một ngưỡng phần trăm cho trước chưa.
  - 🔧 `is_exhausted` L271-273 — Kiểm tra xem token quota của session đã bị dùng hết hoàn toàn chưa.
  - 🔧 `usage_summary` L285-290 — Xây dựng chuỗi tóm tắt ngắn, dễ đọc về token usage so với quota.
  - 🔧 `get_job_breakdown` L317-350 — Tổng hợp các entry token usage cho một chuỗi job cụ thể thành bảng phân tích theo từng provider…
  - 🔧 `format_job_footer` L353-384 — Định dạng tin nhắn footer tóm tắt chi phí token và phân tích theo provider cho một job đã hoàn tất…


---

## Test Suite
_Bộ test end-to-end kiểm chứng pipeline orchestrator, pipeline sinh content, web API, và hành vi menu persona._

### `test_orchestrator_e2e.py`
Bộ test end-to-end cho pipeline orchestrator multi-agent, dùng helper MockBehavior để stub các lệnh gọi skill/router và kiểm tra luồng chạy thành công, xử lý lỗi, và benchmark độ trễ.
  - 🧱 `MockBehavior` L51-103 — Class helper test cấu hình phản hồi/hành vi mock cho run_skill và router_call…
  - 🔧 `_make_session` L1-1 — Xây dựng object Session mock mới, đã điền sẵn dữ liệu profile để dùng xuyên suốt orchestr…
  - 🔧 `test_1_happy_path` L1-1 — Kiểm tra pipeline orchestrator chạy thành công end-to-end trong điều kiện mock thông thường…
  - 🔧 `test_6_latency_benchmark` L1-1 — Benchmark độ trễ chạy pipeline trong điều kiện mock để phát hiện các vấn đề hiệu năng orchestration…
  - 🔧 `run_all` L1-1 — Điểm vào chạy test, thực thi toàn bộ hàm test e2e orchestrator đã định nghĩa theo trình tự…

### `tests/test_web_api.py`
Unit test kiểm chứng web dashboard API expose đúng các route cốt lõi và route editor, và các endpoint business-data suy giảm nhẹ nhàng khi Supabase không khả dụng.
  - 🔧 `_paths` L24-25 — Helper test trích xuất tập đường dẫn route từ bảng route của API.
  - 🔧 `test_core_routes_present` L28-32 — Kiểm chứng các route API dashboard cốt lõi đã được đăng ký.
  - 🔧 `test_editor_routes_present` L35-40 — Kiểm chứng các route API liên quan đến content-editor đã được đăng ký.
  - 🔧 `test_business_degrades_without_supabase` L43-52 — Kiểm chứng các hàm API business-data (skill run, rate, save, list versions, patch) v…
  - 🔧 `test_standalone_build_has_doc_reader` L55-59 — Kiểm chứng bản build ứng dụng standalone có bao gồm component đọc tài liệu.

### `tests/test_persona_menu_changes.py`
Bộ test bao quát hành vi menu/keyboard persona, tra cứu task registry, xlsx import/export khứ hồi, sinh gợi ý key-message, định dạng video script, và trích xuất quy tắc do/don't của brand voice…
  - 🔧 `_all_callbacks` L15-16 — Helper test trích xuất toàn bộ giá trị callback_data từ một inline keyboard markup để kiểm…
  - 🔧 `test_video_scripts_msg_type_drives_format` L87-108 — Kiểm chứng định dạng tin nhắn người dùng do VideoScriptsSkill sinh ra thay đổi tùy theo…
  - 🔧 `test_xlsx_reader_roundtrip` L158-172 — Kiểm tra một workbook xlsx trong bộ nhớ có thể được viết ra rồi đọc lại thành markdown mà không…
  - 🔧 `test_bv_do_dont_parsers` L184-198 — Kiểm tra nội dung markdown brand-voice được phân tích cú pháp đúng thành danh sách quy tắc 'do' và 'don't'…

### `__init__.py`
Marker package rỗng cho module tests; không có nội dung thực thi.

### `test_content_pipeline_e2e.py`
Bộ test end-to-end (16 kịch bản) kiểm chứng hành vi pipeline sinh content: chèn calendar/tone/business-view vào prompt skill, xử lý phạm vi theo tuần, và quy tắc validate, dùng fake…
  - 🧱 `_FakeProfile`  — Test double mô phỏng một business profile với industry, goals, USP, budget, và channel…
  - 🧱 `_FakeSession`  — Test double mô phỏng một session người dùng, giữ profile, intake đang chờ, tone calibration,…
  - 🧱 `_FakeSkill`  — Test double mô phỏng một skill sinh content, ghi lại context và tin nhắn người dùng…
  - 🧱 `_FakeBV`  — Test double mô phỏng một object business-view với kiểm tra rỗng và render khối prompt…
  - 🔧 `_capture_injected`  — Helper test gọi các builder context/message của fake skill và ghi lại calenda…
  - 🔧 `_calendar_max_week_from_text`  — Helper test phân tích cú pháp số thứ tự tuần lớn nhất được tham chiếu trong văn bản calendar bằng regex.
  - 🔧 `run_all`  — Test runner async tùy biến, tìm và thực thi toàn bộ hàm test_* trong module, …


---

## Configuration, Deployment & Product Docs
_Các entry point runtime ở root và file env/config (config.py, run_local.py, run_web.py, .env.example, railway.json, Procfile), workflow deploy GitHub Pages, và tài liệu sản phẩm/kiến trúc, sao…_

### `config.py`
Module cấu hình trung tâm nạp biến môi trường và hằng số (API key, token, feature flag) dùng xuyên suốt toàn bộ ứng dụng.

### `run_local.py`
Entry point chạy local cho Telegram bot: khởi tạo storage pool/DB, đăng ký toàn bộ handler command/message/callback trên một Application python-telegram-bot, và chạy polling.
  - 🔧 `post_init` L50-55 — Hook chạy sau khi khởi tạo, thực hiện sau khi Telegram Application được dựng xong; khởi tạo DB…
  - 🔧 `main` L58-95 — Xây dựng Telegram Application, đăng ký toàn bộ handler command/message/callback, và khởi…

### `run_web.py`
Entry point standalone để chỉ chạy server web dashboard (không cần token Telegram bot), mặc định dùng store nền SQLite với backend Supabase tùy chọn và listener realtime.
  - 🔧 `lifespan` L30-37 — Context manager quản lý lifespan ứng dụng, cấu hình và khởi tạo web store và la…
  - 🔧 `oauth_fb_callback` L40-47 — Xử lý redirect OAuth Facebook cho server dashboard standalone, hoàn tất việc trao đổi token…
  - 🔧 `main` L59-62 — Đọc biến môi trường PORT và khởi động server uvicorn cho dashboard standalone…

### `content_generation_template.xlsx`
Template workbook Excel dùng như định dạng input/output có cấu trúc cho workflow sinh content (ví dụ content calendar hoặc các dòng campaign plan).

**Tài liệu:** `DEPLOY_RAILWAY.md`, `README.md`, `requirements.txt`, `docs/ADS_SCHEDULER_SETUP.md`, `docs/APP_FUNCTIONS.md`, `docs/BACKLOG.md`, `docs/DB_V2_MIGRATION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md`, `docs/LAUNCH_CHECKLIST.md`, `docs/MAX_CMO_REFERENCE.md`, `docs/SCALE_BACKLOG.md`, `docs/markos-flow.md`, `docs/web/ARCHITECTURE.md`, `docs/web/DECISIONS.md`, `docs/web/PRODUCT.md`, `docs/web/ROADMAP.md`, `docs/web/WAY_OF_WORKING.md`, `docs/web/slices/M0-directional-reframe.md`, `docs/web/slices/M0-discovery-to-strategy.md`, `docs/web/slices/M1-content-production.md`, `docs/web/slices/output-reader-editor.md`, `plan.md`, `todo.md`


---

## AI Dev-Assistant Tooling
_Tài sản plugin Claude Code đóng gói trong repo (.claude/) — agent tùy biến, slash command, và bộ skill hỗ trợ phát triển project này, không thuộc phần runtime sản phẩm khi triển khai._

### `.claude/skills/idea-refine/scripts/idea-refine.sh`
Shell script helper cho skill idea-refine, đảm bảo thư mục docs/ideas tồn tại và báo cáo trạng thái sẵn sàng dưới dạng JSON.

### `.claude/skills/LICENSE_agent-skills`
Văn bản license MIT cho bộ sưu tập Agent Skills được vendor (của Addy Osmani) đóng gói trong .claude/skills.

**Tài liệu:** `.claude/agents/code-reviewer.md`, `.claude/agents/security-auditor.md`, `.claude/agents/test-engineer.md`, `.claude/agents/web-performance-auditor.md`, `.claude/commands/build.md`, `.claude/commands/code-simplify.md`, `.claude/commands/plan.md`, `.claude/commands/review.md`, `.claude/commands/ship.md`, `.claude/commands/spec.md`, `.claude/commands/test.md`, `.claude/commands/webperf.md`, `.claude/skills/idea-refine/SKILL.md`, `.claude/skills/idea-refine/examples.md`, `.claude/skills/idea-refine/frameworks.md`, `.claude/skills/idea-refine/refinement-criteria.md`, `.claude/skills/api-and-interface-design/SKILL.md`, `.claude/skills/browser-testing-with-devtools/SKILL.md`, `.claude/skills/ci-cd-and-automation/SKILL.md`, `.claude/skills/code-review-and-quality/SKILL.md`, `.claude/skills/code-simplification/SKILL.md`, `.claude/skills/context-engineering/SKILL.md`, `.claude/skills/debugging-and-error-recovery/SKILL.md`, `.claude/skills/deprecation-and-migration/SKILL.md`, `.claude/skills/documentation-and-adrs/SKILL.md`, `.claude/skills/doubt-driven-development/SKILL.md`, `.claude/skills/frontend-ui-engineering/SKILL.md`, `.claude/skills/git-workflow-and-versioning/SKILL.md`, `.claude/skills/incremental-implementation/SKILL.md`, `.claude/skills/interview-me/SKILL.md`, `.claude/skills/observability-and-instrumentation/SKILL.md`, `.claude/skills/performance-optimization/SKILL.md`, `.claude/skills/planning-and-task-breakdown/SKILL.md`, `.claude/skills/security-and-hardening/SKILL.md`, `.claude/skills/shipping-and-launch/SKILL.md`, `.claude/skills/source-driven-development/SKILL.md`, `.claude/skills/SOURCE.md`, `.claude/skills/spec-driven-development/SKILL.md`, `.claude/skills/test-driven-development/SKILL.md`, `.claude/skills/using-agent-skills/SKILL.md`
